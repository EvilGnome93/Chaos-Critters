# Chaos Critters — Dev-status

Dit document vat samen wat er staat en waarom, zodat een nieuwe Claude Code-sessie
(op deze of een andere locatie) snel weer aanhaakt. Voor het volledige ontwerp: zie
`docs/chaos-critters-projectbrief.md`.

## Setup

- **Repo**: `EvilGnome93/Chaos-Critters` (hernoemd vanaf `gamename-bot`), branch `dev` is actief werk, `main` is voor later/productie.
- **Stack**: Python + discord.py 2.7, PostgreSQL via SQLAlchemy async (asyncpg) + Alembic migraties.
- **Migraties draaien automatisch bij opstart** (`bot.py`, `_draai_migraties()`): vóórdat de bot connect, draait hij `alembic upgrade head` als subprocess tegen de eigen `DATABASE_URL`. Lukt dat niet, dan crasht de bot bewust meteen (geen silent start op een verouderd schema). Dit loste een incident op (2026-07-21): een migratie was gepusht maar nog niet tegen Railway's DB gedraaid, waardoor `/werk`/`/verzorg`/`/lijst` crashten met `UndefinedColumnError`. Je hoeft dus niet meer handmatig `alembic upgrade head` te draaien na een deploy — wel nog steeds nodig als je zelf lokaal migraties wil testen vóórdat je pusht.
- **Lokaal draaien**: `venv/Scripts/python.exe bot.py` vanuit `gamename-bot/`. **Let op**: niet lokaal draaien terwijl Railway ook live staat — beide processen proberen dezelfde Discord-interacties te beantwoorden en botsen (zie git-log rond "Race condition" en de `/werk` ephemeral-fix).
- **Environments**: production op Railway draait met **`ENVIRONMENT=prod`** (niet `production`); alle checks in de code zijn dan ook "is het `dev` of niet", zodat beide spellingen werken. `ENVIRONMENT=dev` in `.env`/Railway-variables schakelt: instant guild-command-sync (`DEV_GUILD_ID`), snellere test-cycli (werk-shifts 1 minuut i.p.v. uren, spawn-tijd-trigger elke 30 min i.p.v. 2-4 uur), en dev-logging naar console.
- **Railway**: er draait **naast dev ook een echte production-omgeving** (bevestigd door de gebruiker, 2026-07-29 — een oudere notitie hier beweerde nog dat er geen aparte prod-bot-service was; dat klopte niet meer). `DATABASE_URL` op Railway zelf gebruikt de interne referentie; lokaal `.env` gebruikt de publieke proxy-URL (interne hostname is niet bereikbaar van buiten Railway's netwerk). Prod draait op `main`, dev op `dev` — het web-adminpanel hoort alleen op prod (zie hieronder).
- **Testgroep heeft momenteel allemaal admin-rechten** op de test-server. Dat betekent dat de `default_permissions(administrator=True)`-restrictie op admin-commando's (`/spawn`, `/give`, `/instelling`, `/setlog`, `/tests`) nog niet getest is tegen een niet-admin gebruiker — en dat testers per ongeluk admin-commando's kunnen uitvoeren (bijv. `/spawn`) zonder dat dat een bug is.
- **Web-adminpanel** (2026-07-29): `portal/` draait als aiohttp-server ín het bot-proces en serveert `web/admin.html` op `/`. Bedoeld domein: `critters.casualchaos.nl` (custom subdomein op Railway). **Setup-stappen staan in `docs/admin-portal-setup.md`** — zolang de env-vars ontbreken start de portal niet en logt de bot alleen een waarschuwing. **Alleen voor prod** (verzoek van de gebruiker): `PORTAL_ENABLED` staat standaard uit zodra `ENVIRONMENT=dev`, óók als de OAuth-vars daar gezet zijn, zodat er nooit twee panelen op dezelfde database kunnen draaien. Expliciet `PORTAL_ENABLED=true` zetten overrulet dat, om in dev te kunnen testen.
- **Wat overleeft een redeploy** (gecheckt 2026-07-29 n.a.v. een vraag): alle tijdgebonden staat zit in Postgres en wordt lazy berekend, dus **werkende pets lopen gewoon door** (`werk_gestart_op`), net als honger/energie-verval (`laatste_verzorging_op`), blessures, slaap-cooldown en ranked-reset. Een shift die tíjdens de downtime afloopt is ook veilig: de notificatie-loop zoekt bij elke tick naar `status=werkplek` + `werk_notificatie_verstuurd=False` en haalt de achterstand vanzelf in. Portal-logins overleven ook (daarom DB-backed). **Actieve spawns overleven sinds 2026-07-30 ook een herstart** (`actieve_spawns`-tabel, zie hieronder) — dat was hiervoor de grootste uitzondering. **Wat wél verdwijnt**: alle open knoppen/dropdowns (lopend `/vecht`, onbeantwoord `/trade`, `/craft`-bevestiging, paginering → "This interaction failed", commando opnieuw uitvoeren lost het op; bij een half gevecht is er nog niks uitgekeerd dus geen scheve MMR), en de berichtentellers voor de activiteit-spawntrigger (die begint na een deploy dus opnieuw te tellen).
- **Database volledig gereset (2026-07-22)**: `spelers`, `huisdieren`, `inventaris`, `log_channels` en `spawn_kanalen` zijn geleegd (TRUNCATE ... RESTART IDENTITY CASCADE) op verzoek van de gebruiker, voor de invoering van per-speler pet-nummers (zie hieronder). Statische content (tiers/pet_soorten/werkplekken/items/instellingen) bleef staan. **`/setlog` en `/setspawnkanaal` moeten na deze reset opnieuw ingesteld worden.**

## Wat werkt (getest, op dev)

- **`/ping`** — connectiviteitscheck.
- **`/todo`, verwijderd (2026-07-30, verzoek van de gebruiker)**: bestond als publiek overzicht van de backlog, maar is helemaal weggehaald (commando + `TODO_ITEMS` uit `cogs/algemeen.py`). "Wat is er nieuw"-communicatie loopt voortaan via `/changelog` (zie hieronder); de backlog zelf blijft alleen hier in dev-status.md staan, niet meer player-facing.
- **`/commands`** (2026-07-28, verzoek van de gebruiker) — ephemeral, volledig commando-overzicht, losgetrokken uit `/tests` (die herhaalde anders elke keer dezelfde ~22 commando's). Lijst (`TEST_COMMANDOS`) verhuisd van `cogs/admin.py` naar `cogs/algemeen.py`, samen met dit nieuwe commando.
- **`/vang <naam>`** — vangt de actief gespawnde pet in het kanaal (exacte naam, of het deel vóór haakjes bij soorten als "Hond (Zwerfhond)" → "Hond"). Geen spawn actief = nette foutmelding.
- **Spawn-systeem**: activiteit-trigger (25-40 berichten, instelbaar via `Instelling`-tabel) + tijd-trigger (2-4u, 30 min in dev), meerdere spawn-kanalen per server (`/setspawnkanaal`, `/verwijderspawnkanaal`). Eén actieve spawn per kanaal; een nieuwe spawn markeert de oude als "ontsnapt" in de embed (race-condition-veilig via een lock per kanaal). Spawn-embed toont tier-kleur + pet-afbeelding (of placeholder). Bij vangst wordt dezelfde embed geëdit naar "gevangen door X", geen apart bericht meer.
- **`/spawn`** (admin) — forceert een spawn, met optionele `tier`- en `naam`-parameters (naam heeft autocomplete).
- **Werk-laag**: `/werk pet_id werkplek cyclus` wijst toe; `/werk pet_id` (zonder extra params) haalt op als de shift klaar is, of toont resterende tijd. Elke werkplek levert een eigen grondstof-item + Chaos Coins (was "currency", puur weergavenaam gewijzigd — DB-kolom heet nog `currency`). Energie wordt volledig afgetrokken bij start. Notificatie: tag in het kanaal waar de shift gestart is (niet DM) zodra klaar. **Max 3 pets tegelijk aan het werk per speler** (2026-07-22, instelling `max_werkende_pets_per_speler` in de `Instellingen`-tabel, later op te hogen zonder codewijziging) — een per-speler-limiet. **Werkplekken uitbreiden, afgerond (2026-07-26, verzoek van de gebruiker)**: het bestaande `capaciteit`-veld op `Werkplek` wordt nu écht afgedwongen als **gedeelde limiet over alle spelers heen** (`cogs/werk.py:_aantal_werkend_op_werkplek`, telt alle pets ongeacht eigenaar met `status == werkplek` op die werkplek); is de werkplek vol, dan krijgt de speler een duidelijke foutmelding met bezetting (bijv. "1/1 bezet") en moet het later opnieuw proberen — geen wachtrij. **Tweede grondstof per werkplek**: elke werkplek heeft nu ook een zeldzamere bonus-grondstof (`Werkplek.opbrengst_item_2_id`) met een kleine kans per voltooide shift (`Werkplek.opbrengst_2_kans`, placeholder 25% voor iedereen) — Moestuin→Fruit, Vijver→Water, Werkbank→Spijker, Bos→Bladeren, Nachtwacht→Sterrenstof, Mijnschacht→Edelsteen (vaste hoeveelheid `BONUS_GRONDSTOF_AANTAL` = 1 per geslaagde roll, apart vermeld in bericht/log met een 🍀). **Bugfix**: `/werk`'s werkplek-dropdown miste de al langer bestaande "Mijnschacht"-werkplek (met Erts als grondstof, voorkeur van Chaos Mol/Das/Varken in `scripts/seed.py`) — die 3 pet-soorten konden er dus nooit heen; toegevoegd aan de keuzelijst. Getest via `scripts/test_werk.py`: gedeelde capaciteit blokkeert een 2e speler zodra een werkplek (Nachtwacht, capaciteit 1) vol zit; Mijnschacht-shift werkt end-to-end; de bonus-grondstof-roll is getest met geforceerde `random.random()`-waarden voor zowel een gegarandeerde als een gegarandeerd gemiste roll.
- **Item-overhaul deel 1: voerbakken/Zelfreinigend systeem, afgerond (2026-07-27, verzoek van de gebruiker)**: Simpele/Slimme voerbak en Zelfreinigend systeem waren al koopbaar via `/shop` maar deden niets — hebben nu een echt, **per pet uit te rusten** effect (bewuste keuze: account-breed passief was simpeler geweest, maar de gebruiker koos expliciet voor per-pet uitrusten). Nieuw commando `/uitrusten pet_id item [afkoppelen]`: rust een pet uit (verbruikt 1 exemplaar uit je inventaris) of koppelt het weer af (komt terug in je inventaris). Voerbakken zitten in hetzelfde slot (`Huisdier.voerbak_niveau`, "simpel"/"slim"/None) — een nieuwe uitrusten wisselt de oude automatisch terug naar je inventaris; Zelfreinigend systeem heeft een eigen slot (`Huisdier.zelfreinigend_actief`). **Effecten** (`utils/stats.py:sync_stats`, placeholder-waarde `SIMPELE_VOERBAK_FACTOR` = 2) — **let op, 2026-07-27 omgedraaid t.o.v. de eerste versie** (feedback van de gebruiker na een live-test: "is het niet veeel logischer dat de voerbak, voer geeft?" — terechte correctie, een voerbak moet honger geven, geen energie): een voerbak is letterlijk een voerbak en geeft passief **honger** terug (Simpele voerbak vult de helft van het normale verval aan, Slimme voerbak het volledige verval, dus netto stabiele honger); Zelfreinigend systeem heeft het effect overgenomen dat eerst aan de voerbak hing — laat **energie** ook buiten rust (bijv. tijdens werk) passief herstellen, alsof het systeem de pet automatisch onderhoudt zonder dat 'ie hoeft uit te rusten (één item, geen tiers, dus vol tempo zodra actief). Het beloofde "verhoogt blijdschap" is dus niet gebruikt (blijdschap blijft bewust gepauzeerd, zie boven). **Slimme voerbak kost nu ook grondstoffen**: `/shop` vereist bij aankoop ook 5x Schroot naast de 250 Chaos Coins (`RECEPT_KOSTEN` in `cogs/verzorging.py`, generiek genoeg om later meer recepten toe te voegen) — geeft Schroot als eerste van de 12 grondstof-soorten een concreet doel. Uitrusting zichtbaar in `/verzorg` (bekijk-modus) en `/lijst`. Getest via `scripts/test_item_overhaul.py`: een pure unit-test bevestigt de energie/honger-effecten in `sync_stats`; een end-to-end test bevestigt de Schroot-kostencheck bij `/shop`, en equip/auto-swap/afkoppelen via `/uitrusten` inclusief dat afgekoppelde/vervangen items echt terugkomen in de inventaris. **Voerbak-effect nogmaals herzien (2026-07-28, verzoek van de gebruiker: "hij gebruikt het voer dat je hebt in je inventaris, i.p.v. het huidige idee")**: de vorige versie gaf een abstracte honger-regen los van je inventaris (Simpele voerbak vulde de helft van het verval aan, Slimme het volledige) — nu verbruikt de voerbak écht voedingsitems. Nieuwe async functie `utils/stats.py:sync_stats_met_voerbak(session, huisdier)` (roept de bestaande, nog steeds pure `sync_stats()` aan voor het normale verval/Zelfreinigend-effect, en vult daarna honger aan door voedingsitems uit de inventaris te verbruiken tot 100 of tot het op is) — **Simpele voerbak mag alleen Basis brokjes gebruiken, Slimme voerbak alle 3 "echte" voedingsitems (Basis brokjes/Graanvrije premium voeding/Vers vlees/vis), altijd goedkoopste eerst**; de Mysterie voedselzak wordt bewust nooit automatisch gebruikt (blijft een handmatige gok). **Geen fallback**: geen voer meer in de inventaris betekent gewoon geen effect (normaal verval), geen kunstmatige regen als vangnet. `HONGER_HERSTEL_WAARDEN`/`VOLLEDIG_HERSTEL_ITEMS` verhuisd naar `utils/stats.py` als single source of truth (hergebruikt door `cogs/verzorging.py` voor `/verzorg`'s handmatige voeden) — nodig omdat `cogs/werk.py` dit niet uit `cogs/verzorging.py` mag importeren (circulaire import: `verzorging.py` importeert al van `werk.py`). Alle plekken die pets aanraken (`/lijst`, `/verzorg`, `/slaap`, `/werk`, `/team`, `/vecht`, PvP-acceptatie) zijn overgezet van `sync_stats()` naar `sync_stats_met_voerbak()`. `SIMPELE_VOERBAK_FACTOR` (de oude regen-snelheid-constante) is verwijderd, niet meer relevant. Getest via een uitgebreide `test_voerbak_verbruikt_echt_voer()` in `scripts/test_item_overhaul.py`: geen voer → geen effect; Simpele voerbak verbruikt Basis brokjes; Simpele voerbak laat Graanvrije premium voeding bewust liggen ook als dat de enige beschikbare voeding is; Slimme voerbak gebruikt Vers vlees/vis (volledig herstel) als dat het enige beschikbare voer is. Regressie gecheckt met de volledige testsuite.
- **Item-overhaul deel 2: doel voor alle grondstoffen, afgerond (2026-07-27, verzoek van de gebruiker: recept-kosten aan bestaande items hangen i.p.v. een nieuw crafting-systeem)**: de overige 11 grondstoffen krijgen nu allemaal een doel via `RECEPT_KOSTEN` in `cogs/verzorging.py` (hetzelfde generieke patroon als Schroot → Slimme voerbak, geen codewijziging nodig geweest om te hergebruiken): Water → Simpele voerbak (2x), Sterrenstof → Zelfreinigend systeem (2x), Groente → Graanvrije premium voeding (3x), Fruit → Mysterie voedselzak (1x), Algen → Vers vlees/vis (3x), Takken → Naamkaartje (3x), Bladeren → Focus drankje (2x), en twee multi-ingrediënt recepten: Werk-elixer kost nu Erts (3x) + Spijker (2x), Extra match token kost nu Maanschijnkristal (2x) + Edelsteen (1x). Hoeveelheden lager gehouden voor de zeldzamere bonus-grondstoffen (25%-kans-per-shift) dan voor de gegarandeerde hoofdgrondstoffen. Shop-beschrijvingen vermelden de recept-kosten (zelfde format als Slimme voerbak). **Tegelijk de bekende ranked-limiet-omzeiling aangepakt** (zie "Bekende balans-issues"): Extra match token's prijs ging van 50 naar 150 Chaos Coins, plus het nieuwe Maanschijnkristal/Edelsteen-recept — een token kost nu ook gerichte werk-tijd, niet meer alleen losse winst-currency. Getest via nieuw `scripts/test_grondstof_recepten.py`: verifieert dat alle 12 grondstof/materiaal-items in minstens 1 recept voorkomen, test het multi-ingrediënt-pad van Werk-elixer (mislukt op het 2e ingrediënt als dat ontbreekt, correcte aftrek van beide bij succes), en bevestigt Extra match token's nieuwe prijs + recept. Regressie gecheckt met `scripts/test_item_overhaul.py` (aangepast: Simpele voerbak kost nu ook Water, test geeft dat vooraf), `test_werk.py`, `test_elementen.py`, `test_trading.py`, `test_vecht_friendly.py`.
- **Clan-systeem, afgerond (2026-07-27, verzoek van de gebruiker: "laten we het nu doen" i.p.v. de geplande volgende sessie)**: heette tijdens het bouwen eerst "gilde" (Nederlands, en de originele naam in de projectbrief), maar hernoemd naar **"clan"** — "guild" zou de voor de hand liggende Engelse vertaling zijn geweest, maar discord.py gebruikt die naam al voor een Discord-server zelf (`discord.Guild`/`interaction.guild_id`), wat verwarrend zou zijn naast onze eigen speler-groepering. Nieuwe `Clan`-tabel (`db/models.py`, migratie `f52f01a95370`) met `naam` (uniek), `oprichter_id`, `totale_werk_opbrengst`. `Speler.clan_id` (bestond al als ongebruikt placeholder-veld sinds het begin van het project, heette toen `gilde_id`) is nu een echte FK naar `clans.id` i.p.v. een los BigInteger-veld. Nieuwe cog `cogs/clan.py` met `/clan-aanmaken naam`, `/clan-join naam` (autocomplete), `/clan-verlaten` (clan ontbindt automatisch als het laatste lid weggaat), `/clan-ontbinden` (alleen de oprichter, ontbindt meteen ook met leden erin), `/clan-info [naam]` (oprichter/ledenaantal/ledenlijst/werk-opbrengst) en `/clan-leaderboard` (top 10 op werk-opbrengst, medailles voor top 3). **Gedeelde werkplekken**: elke clan krijgt zijn eigen capaciteit-pool per werkplek, volledig los van andere clans én van clanloze spelers (`cogs/werk.py:_aantal_werkend_op_werkplek`, nu gescoped op `Speler.clan_id` i.p.v. één globale telling) — clanloze spelers delen samen de oorspronkelijke "None"-pool, dat gedrag blijft ongewijzigd. **Leaderboard-metric**: cumulatieve Chaos Coins-opbrengst van `/werk`-shifts van alle leden samen (`Clan.totale_werk_opbrengst`, opgehoogd in `cogs/werk.py:_verwerk_lopende_shift`) — blijft staan ook als leden het geld weer uitgeven, dus meet productiviteit, niet spaarsaldo. Bewuste scope-keuzes (met de gebruiker afgestemd): losstaand in-bot lidmaatschap (geen Discord-rol-koppeling), geen kick/invite-systeem, alles in één keer gebouwd i.p.v. gefaseerd. Getest via nieuw `scripts/test_clan.py`: aanmaken/dubbele-naam-weigering/joinen, capaciteit-isolatie tussen 2 clans + de clanloze pool (met een defensieve check tegen de gedeelde live dev-DB, waar een echte tester toevallig al een slot bezet kan houden), werk-opbrengst-optelling + leaderboard-weergave, en auto-ontbinden bij leegloop vs. expliciet `/clan-ontbinden` door (niet-)oprichters. Regressie gecheckt met `test_werk.py`, `test_item_overhaul.py`, `test_grondstof_recepten.py`, `test_elementen.py`, `test_trading.py`, `test_vecht_friendly.py`, `test_pvp_namen.py`.
- **`/lijst`** — gepagineerd (10/pagina) overzicht van je pets, publiek zichtbaar, met sorteerknoppen (ID/Level/Naam/Werkstatus + Honger/Energie, elk een eigen kleur, actieve sortering met ✅-prefix), resterende werktijd per werkende pet, level+XP, en per pet de actuele honger/energie. Sortering op honger/energie is oplopend (laagste/meest urgent te verzorgen eerst).
- **Discord-logsysteem**: `/setlog <categorie> <kanaal>` (admin) koppelt per server+categorie een logkanaal (`utils/discord_log.py`, patroon overgenomen van het Botv3-project maar herbouwd in Python). Categorieën in gebruik: `main` (bot start/fouten), `vangst` (catches + geforceerde spawns), `werk` (start/opbrengst).
- **5 tiers, 150 pet-soorten in `scripts/seed.py`**. **25 soorten toegevoegd (2026-07-27)** — 4 namen van de gebruiker (Steur, Mees, Schotse hooglander, Koi-Karper), aangevuld met 21 eigen soorten tot 25 (Aardvarken, Kameel, Zebra, Maki, Walvis, Kwokka, Klipdas, Piranha, Zeester, Neusaap, Okapi, Wombat, Zeekomkommer, Rendier, Zeeleeuw, Anemoon, Pissebed, Vuurvis, Blobvis, Mantisgarnaal, Pijlgifkikker). **Lemuur → Maki hernoemd (2026-07-27, feedback van de gebruiker: "Maki" is de volksmondnaam)** — rechtstreeks in de DB hernoemd, geen gevangen exemplaren op dat moment dus geen impact op bestaande Huisdier-records. **Mol vervangen door Aardvarken** (feedback van de gebruiker toegepast vóór het toevoegen: Mol zou de bestaande Chaos Mol dubbel maken). **Schotse hooglander bewust wél gehouden** ondanks overlap met Bizon/Chaos Stier (runderachtigen) — expliciet verzoek van de gebruiker, uitzondering op de anders geldende avoid-overlap-regel. Verdeeld als 6 Common/6 Uncommon/7 Rare/4 Epic/2 Legendary (Walvis, Blobvis) — Blobvis bewust met de laagste basisstats van de hele lijst maar wel Legendary-tier, als bewuste grap (irl beroemd om zijn slappe, weinig indrukwekkende uiterlijk). Elementen toegewezen volgens hetzelfde thema-patroon (12 grond, 8 water, 1 lucht, 2 vuur, 2 chaos) — nieuwe totalen: 62 grond, 34 water, 20 lucht, 14 vuur, 20 chaos. Afbeeldingen voor deze 25 toegevoegd en gekoppeld (2026-07-27) — alle 150 soorten hebben nu een eigen afbeelding, geen placeholders meer. Koi-Karper's slug gecorrigeerd van `koi_karper` naar `koi-karper` (mismatch met het aangeleverde bestandsnaam-streepje). Afbeeldingen bewust eerst gepusht naar GitHub, dan pas gekoppeld (zelfde reden als de cache-waarschuwing hieronder). **25 soorten toegevoegd (2026-07-24)** — 19 namen kwamen van de gebruiker (Bizon, Luiaard, Koala, Pinguin, Kalkoen, Schildpad, Dolfijn, Rog, Lieveheersbeestje, Naaktslak, Orang oetan, Chimpansee, Libelle, Bij, Zeester, Sprinkhaan, Papegaai, Panda, Vogelbekdier), zelf verdeeld over tiers (9 Common, 5 Uncommon, 5 Rare) op basis van kracht/zeldzaamheid-gevoel; aangevuld met 6 eigen soorten tot 25: Garnaal (Common), Antilope + Kiwi (Uncommon), Zwaardvis + Wasbeerhond (Rare), Walvis (Epic). Let op: de gebruikerslijst had zelf al familie-overlap met bestaand werk (Panda naast Beer/IJsbeer, Orang oetan + Chimpansee naast Gorilla, Papegaai naast Parkiet, Naaktslak naast Slak) — bewust niet gecorrigeerd op expliciet verzoek van de gebruiker, wel over verschillende tiers verdeeld om het te verzachten. Afbeeldingen voor deze 25 toegevoegd en gekoppeld (2026-07-24) — alle 125 soorten hebben nu een eigen afbeelding, geen placeholders meer. **Sprinkhaan, Papegaai en Wasbeerhond omgezet naar Chaos-varianten** (2026-07-24, verzoek van de gebruiker) — nu **Chaos Sprinkhaan**, **Chaos Papegaai**, **Chaos Wasbeerhond** met GEMIDDELD/GEMIDDELD placeholder-stats en "wisselt dagelijks"-flavor, zelfde patroon als de overige Chaos-soorten. Rechtstreeks in de DB hernoemd (niet verwijderd+opnieuw geseed), dus blijft hetzelfde record ook als er ooit een gevangen exemplaar van was. **Nog 3 vervangingen** (2026-07-24, verzoek van de gebruiker): Walvis → **Rode Panda** (Epic, Bos), Zeester → **Gordeldier** (Common, geen werkplek-voorkeur), Naaktslak → **Ekster** (Common, kleine kans op bonus-item bij werk — losse diefstal-flavor, geen werkplek-voorkeur). Zelfde aanpak: rechtstreeks hernoemd in de DB, totaal blijft 125. **10 soorten toegevoegd (2026-07-24)** om de kans-per-soort tussen tiers gelijkmatiger te maken: was een knik van 1,67% (Uncommon) naar 0,86% (Rare) per soort, nu 1,41/1,39/0,82/0,56/0,25% over Common→Legendary. Verdeeld als 5 Common (Krekel, Vlinder, Worm, Kakkerlak, Vlo — bewust een nieuwe "ongedierte"-hoek), 3 Uncommon (Stinkdier, Kolibrie, Buidelrat), 1 Rare (Axolotl), 1 Epic (Struisvogel). Legendary bewust ongewijzigd (12 soorten, 0,25% per soort — uiterst zeldzaam is hier gewenst). **Tiers uitgebreid van 3 naar 5 (2026-07-24)**: naast Common/Rare/Legendary nu ook **Uncommon (id 2)** en **Epic (id 4)** — de tier-id's lagen bewust uit elkaar (1/3/5) voor precies dit moment, dus geen schemawijziging nodig. Nieuwe verdeling: Common 45%/1.0x, Uncommon 25%/1.2x, Rare 18%/1.4x, Epic 9%/1.7x, Legendary 3%/2.0x (was 70/25/5 met 1.0/1.4/2.0) — `scripts/seed.py:seed()` werkt de spawnkans/multiplier van de al-bestaande tiers 1/3/5 nu expliciet bij via `UPDATE`, want `INSERT ... ON CONFLICT DO NOTHING` raakt bestaande rijen niet aan. Tier-kleuren (`TIER_KLEUREN` in `cogs/vangen.py`) en de `/spawn tier`-keuzes in `cogs/vangen.py` zijn aangevuld met Uncommon (groen) en Epic (paars). Elke soort heeft een eigen AI-gegenereerde afbeelding in `docs/assets/`, gelinkt via `scripts/link_afbeeldingen.py` (leest repo-naam automatisch uit de git remote, dus overleefde de repo-rename zonder handwerk). **13 nieuwe soorten toegevoegd (2026-07-21)** — Gans/Marter/Chaos Rat/Eekhoorn/Hagedis/Kever (T1), Valk/Hert/Chaos Bever/Hermelijn (T3), Lynx/Slang/Chaos Zwijn (T5). **14 soorten toegevoegd (2026-07-23)** — Duif/Cavia/Krab/Mier/Chaos Mol/Specht/Slak (T1), Das/Zeehond/Havik/Vleermuis/Chaos Reiger (T3), Beer/Chaos Olifant (T5). **20 soorten toegevoegd (2026-07-24)** — Fret/Schaap/Geit/Kikker/Pauw/Goudvis/Muis/Mus/Chaos Spin (T1), Kraai/Pelikaan/Flamingo/Stekelvarken/Kwal/Zwaan/Chaos Kameleon (T3), Tijger/Panter/Neushoorn/Chaos Giraffe (T5). **Eerste 30 soorten voor Uncommon/Epic toegevoegd (2026-07-24)** — Hamster/Varken/Ezel/Wezel/Bunzing/Zeepaardje/Kraanvogel/Alpaca/Lama/Kwartel/Parkiet/Faisant/Stokstaartje/Chaos Kangoeroe/Chaos Toekan (Uncommon), IJsbeer/Luipaard/Poema/Krokodil/Anaconda/Hyena/Gier/Walrus/Zeekoe/Haai/Veelvraat/Gorilla/Nijlpaard/Chaos Octopus/Chaos Stier (Epic): dit keer volledig zelf verzonnen (naam+stats+beschrijving) op verzoek van de gebruiker, i.p.v. de gebruiker die naam+tier aanleverde. Kip en Buizerd zijn vervangen door Hamster en Zeepaardje (2026-07-24, feedback van de gebruiker: Kip overlapte met de al bestaande Chaos Kip, Buizerd voegde weinig toe naast de vele bestaande roofvogels Valk/Havik/Steenarend/Condor/Gier). Jaguar is vervangen door IJsbeer (2026-07-24, feedback: te veel gelijkenis met Luipaard, beide grote roofkatten). Condor is vervangen door Hyena (2026-07-24, feedback: Condor en Gier waren allebei aasetende roofvogels met een vergelijkbaar silhouet). Griffioen en Chaos Basilisk zijn vervangen door Gorilla en Chaos Stier (2026-07-24, feedback: mythische wezens pasten niet tussen de verder allemaal echte dieren — geldt kennelijk ook voor Chaos-varianten, ondanks dat de al bestaande Chaos Eenhoorn wel mythisch is). **Vanaf nu altijd controleren op dier-familie-overlap én "hoort dit dier hier wel thuis" (niet alleen naam) vóór het voorstellen van nieuwe soorten, ook bij Chaos-varianten**, zie het memory-bestand over pet-diversiteit. Afbeeldingen voor deze 30 toegevoegd en gekoppeld (2026-07-24) — alle 90 soorten hadden op dat moment een eigen afbeelding. Afbeeldingen voor deze 10 toegevoegd en gekoppeld (2026-07-24) — alle 100 soorten hebben nu een eigen afbeelding, geen placeholders meer. Bij alle lichtingen: omgekeerde correlatie gevecht/werk, hoogste tier zonder werkplek-voorkeur (Epic/Legendary). Sinds de derde lichting: `ZEER_LAAG = 10` in de stat-schaal. **Afbeeldingen altijd vóór het spawnen pushen** — Discord's media-proxy cachet ook mislukte fetches per exacte URL; een spawn vlak vóór GitHub's raw-CDN het bestand heeft opgepikt laat het plaatje permanent leeg tot de URL handmatig een cache-buster (`?v=2`) krijgt (gebeurd bij Chaos Mol, 2026-07-23).
- **6 werkplekken geseed**, elk gekoppeld aan een eigen grondstof-item: Moestuin→Groente, Vijver→Algen, Werkbank→Schroot, Bos→Takken, Nachtwacht→Maanschijnkristal (hernoemd 2026-07-21, was Moestuin-oogst/Vijver-extract/Werkbank-materiaal/Bos-oogst/Nachtwacht-extract — via migratie `9d3f2b6a7c41`, want de oude namen stonden al in de live DB), **Mijnschacht→Erts (nieuw, 2026-07-23)** — voorheen was "de Mijnschacht" puur flavor-tekst bij Wasbeer (echte werkplek was Werkbank); nu een echte 6e werkplek, gekoppeld aan Das en Chaos Mol.
- **11 shop-items geseed**, koopbaar via `/shop` (zie hieronder).
- **Stat-verval & `/verzorg`**: honger daalt lazy over tijd (berekend bij elke aanraking van een pet, zoals `/lijst`, `/verzorg`, `/werk` — geen achtergrondtaak, logica in `utils/stats.py`). Energie herstelt passief (+1/10 min, alleen in status `rust`, brief sectie 6), of instant via `/slaap pet_id` (energie naar 100, kost 20 honger, max 1x per dag per pet, dev-versneld). `/verzorg pet_id` toont level+XP+stats; met optioneel `item` (Basis brokjes/Graanvrije premium voeding/Vers vlees/vis/Mysterie voedselzak) verbruikt het 1 stuk uit de inventaris om **honger** te herstellen (was energie, aangepast 2026-07-22 — voelde onlogisch dat voeding tegen energie ging i.p.v. trek). Bij honger=0 kan een pet niet aan het werk gezet worden (zelfde blokkade als energie<20, gedeeld via `inzetbaarheid_probleem()`). Verval-snelheden zijn placeholders, en in dev met dezelfde 120x-versnellingsfactor als de werk-cycli.
- **Blijdschap is geparkeerd (2026-07-22)**: de kolom staat nog in `Huisdier`, maar niets in de bot leest/schrijft 'm meer. Was alleen dalend zonder enige manier om te herstellen, wat als een bug aanvoelde. Terugbrengen zodra er een zinvol herstel-mechanisme is (kandidaat: het Zelfreinigend systeem shop-item, zie backlog-punt 8).
- **`/shop`** — zonder `item` toont het een ephemeral overzicht van de 11 koopbare items (gegroepeerd: Voeding/Boosts/Overig, grondstoffen/materialen niet-koopbaar want die komen alleen uit werken). Met `item` + optioneel `aantal` koopt het tegen Chaos Coins (autocomplete over koopbare items). De 3 "overig"-automatiseringsitems (voerbakken, zelfreinigend systeem) zijn nu wel koopbaar, maar hun passieve herstel-effect is **bewust nog niet geïmplementeerd** — ze belanden alleen in de inventaris, met opzet buiten scope van deze stap gehouden.
- **`/items`** — ephemeral overzicht van je inventaris, gegroepeerd per itemtype (Voeding/Grondstoffen/Materialen/Boosts/Overig) met aantallen.
- **`/craft`, afgerond (2026-07-28, uitkomst van de balans-audit vraag 3)**: aparte, duidelijkere koopflow voor de 10 items met een grondstof-recept (`RECEPT_KOSTEN`). Zonder `item`: overzicht van alle craftbare items + hun volledige kosten. Met `item` + optioneel `aantal`: preview-embed met per regel ✅/❌ of je genoeg Chaos Coins/grondstoffen hebt, en een `CraftBevestigView` met een Bevestigen-knop die uitgeschakeld is zolang je iets tekortkomt. De daadwerkelijke aankoop-logica (`_koop_item` in `cogs/verzorging.py`) is uit `/shop` getrokken naar een gedeelde functie, zodat `/shop` en `/craft` niet uit de pas kunnen lopen — `/shop` blijft ook gewoon werken voor recept-items (met de bestaande foutmelding-bij-tekort). **`/craft-lijst`** (verzoek van de gebruiker, sinds 2026-07-30 definitief permanent, niet meer tijdelijk): toont alleen het overzicht, zonder de rest van `/craft` — hergebruikt dezelfde `_craft_overzicht_embed()`-helper. Getest via nieuw `scripts/test_craft.py`: overzicht, onbekend/niet-craftbaar item, preview met tekort (Bevestigen uitgeschakeld) → aanvullen → bevestigen → daadwerkelijke aftrek, en een regressiecheck dat `/shop` na de refactor nog steeds werkt.
- **`/give`** (admin) — geeft een speler N stuks van een item (autocomplete over alle geseede items). Was bedoeld als tijdelijk testcommando zolang er geen `/shop` was; nu die is er, heroverwegen of dit blijft staan (handig voor toekomstige features testen) of weg mag.
- **`/herstel`** (admin, 2026-07-26, puur testgemak — verzoek van de gebruiker): zet honger + energie terug naar 100. Drie scopes: één specifieke pet (`pet_id`), het hele team (`scope=team`), of alle pets (standaard, geen `pet_id`/`scope`) — optioneel voor een andere speler (`speler`, standaard jezelf). Voorkomt dat je tijdens het testen steeds moet wachten op stat-verval/voeding. Geen speler-facing feature.
- **`/tests`, vervangen door `/changelog` (2026-07-30, verzoek van de gebruiker: "op de zelfde manier als Botv3")**: het oude commando stuurde een `@everyone`-oproep direct, zonder reviewstap. Zie "Nieuw commando `/changelog`" hieronder voor de vervanger.
- **Rol-gebaseerde admin-toegang**: naast Administrator-permissie mag ook de rol uit `ADMIN_ROLE_ID` (env var, nog in te stellen op Railway) alle admin-commando's gebruiken. Eigen check (`utils/checks.py:is_admin`) i.p.v. Discord's `default_permissions`, want die laatste wordt door Discord zelf afgedwongen vóór de bot de interactie ziet — een rol toevoegen kon daarmee niet puur via code.
- **Level-up systeem** (sectie 9): `Huisdier.xp`-veld, XP komt uit voltooide werk-shifts (`utils/leveling.py:voeg_xp_toe`, XP naar rato van de effectieve uren) én sinds 2026-07-22 ook uit gevechten (zie hieronder). Bij `level × 100` XP levelt de pet op: +2% samengestelde groei op gevecht_genen en werk_genen, tot max level 50. Meerdere levels in 1x worden correct verwerkt (getest).
- **Pet-nummers zijn per speler** (2026-07-22): `Huisdier.volgnummer` (uniek per `eigenaar_id`, constraint `uq_huisdieren_eigenaar_volgnummer`) i.p.v. het interne, over alle spelers heen doorlopende `id`. Toegewezen vanuit `Speler.volgend_pet_nummer`, die blijft oplopen (ook na een toekomstige `/release`) zodat nummers nooit hergebruikt/dubbel worden. Alle `pet_id`-parameters (`/werk`, `/verzorg`, `/slaap`) en weergaven (`/lijst`, `/team`, vangst-berichten) gebruiken dit volgnummer, niet de interne `id`.
- **Team & gevechten** (sectie 12/13): `/team` (interactieve Select, max 3 niet-werkende/inzetbare pets) en `/vecht` (gesimuleerde tegenstander op basis van MMR, of een speler uitdagen met optionele inzet in Chaos Coins/items). Een gevecht is best-of-3 van opeenvolgende 1v1-matchups (niet één team-vergelijking zoals de brief letterlijk beschrijft — uitgebreid op verzoek van de gebruiker), met per matchup een tactiek-keuze (Aggressief/Gebalanceerd/Voorzichtig, elk een andere RNG-variantie) of vluchten, en automatisch opgeloste interne rondes met korte animatie. Een verslagen pet krijgt 0 energie + een tijdelijke blessure (`Huisdier.geblesseerd_tot`, gecontroleerd via `inzetbaarheid_probleem()`). MMR-aanpassing via Elo (`utils/gevechten.py:elo_delta`), currency-beloning schalend met tegenstander-MMR, XP naar pets. Dagelijkse gratis ranked-pogingen (`Instelling.ranked_gratis_per_dag`, huidige waarde 3) met het "Extra match token"-item als manier om daaroverheen te gaan. Bij PvP kiezen **beide spelers** per matchup een eigen tactiek (2026-07-22, was eerst alleen de uitdager met de tegenstander altijd passief "gebalanceerd"); een matchup lost pas op zodra beide gekozen hebben, met een "wachten..."-tussenscherm zolang dat niet zo is. Wegrennen werkt voor beide kanten en telt correct als winst voor de ander (fix: dit gaf voorheen altijd een verlies voor de uitdager, ook als de tegenstander vluchtte). PvE blijft ongewijzigd: alleen de speler kiest, tegenstander is een simulatie. Volledig end-to-end getest via scripts (PvE win/verlies/vluchten, PvP met inzet, daglimiet+token, PvP-tactieken van beide kanten, vluchtende tegenstander). **`/vecht` herbouwd naar een paneel voor de inzet (2026-07-26, feedback van de gebruiker: losse `inzet_coins`/`inzet_item`/`inzet_aantal`-parameters waren onoverzichtelijk, zelfde reden als de `/trade`-herbouw)**: `/vecht [tegenstander]` heeft alleen nog de tegenstander als parameter (via Discord's eigen speler-picker); bij een uitdaging opent `VechtInzetView` (ephemeral) — een dropdown met je eigen items (`_inzet_item_opties`) plus knoppen die `VechtAantalModal`/`VechtCoinsModal` openen. Pas bij op "⚔️ Uitdagen" klikken wordt de inzet gevalideerd (voorraad/coins) en de bestaande `UitdagingView`-flow gestart. PvE blijft een direct commando zonder paneel (geen inzet mogelijk). Getest: paneel bouwt de juiste `inzet_coins`/`inzet_item`-waarden op en geeft ze correct door aan `UitdagingView`. **Eindmelding ondubbelzinnig gemaakt bij PvP (2026-07-26, feedback van de gebruiker: "jij wint/verliest" was verwarrend voor de niet-uitdagende speler, aangezien beide spelers naar hetzelfde bericht kijken)**: `_verwerk_einde` gebruikt nu expliciete verwijzingen voor beide spelers i.p.v. "jij" — titel ("🏆 Speler wint!"), eindstand, MMR-verandering (nu voor **beide** spelers getoond, niet alleen de uitdager) en wie de inzet krijgt. Bij PvE (geen tweede echte speler) blijft het gewoon "Jij". **Weergavenaam i.p.v. @-tag, definitieve fix (2026-07-26, verzoek van de gebruiker: een tag is niet nodig, en bleef tóch verschijnen)**: de eerste poging (`_weergavenaam()`, cache-lookup via `guild.get_member()`) werkte onbetrouwbaar omdat `bot.py` de (privileged) members-intent niet aanzet — `get_member()` gaf voor niet-gecachte spelers `None` terug, met een kale `<@id>`-mention als resultaat. Vervangen door weergavenamen die meteen vastgelegd worden vanuit een vers `discord.Member`-object (op het moment dat Discord die altijd compleet aanlevert: bij command-aanroep of de native Member-parameter) en als kale strings doorgegeven door de hele keten `VechtInzetView` → `UitdagingView` → `VechtView` (`uitdager_naam`/`tegenstander_naam`/`eigen_naam`, opgehaald via `VechtView._naam_van()`). Dezelfde ambiguïteit ("jij" voor beide spelers) zat ook in de **rondelogs per matchup**, niet alleen de eindmelding: `speel_matchup()` (`utils/gevechten.py`) kreeg daarom ook `eigen_naam`/`tegenstander_naam`-parameters (default "jij"/"tegenstander", dus PvE-tekst blijft ongewijzigd). Getest via `scripts/test_pvp_namen.py`: een volledig PvP-gevecht (uitdaging → accepteren → alle matchups) vangt alle verstuurde teksten op en controleert dat nergens een kale `<@id>`-mention voorkomt behalve de bewuste ping (`content=...` bij het versturen van de uitdaging, puur om de tegenstander een notificatie te geven). **Race condition bij dubbele matchup-verwerking gefixt (2026-07-26, gemeld door de gebruiker met bewijsmateriaal uit de logs: een gevecht met 3 pets eindigde op een onmogelijke stand als 3-1, en de winnaar klopte niet)**: bij PvP kunnen beide spelers bijna gelijktijdig hun tactiek kiezen; doordat de resolutie van de een async DB-calls doet (`_blesseer`) kon de ander, terwijl de eerste nog bezig was, ook "allebei gekozen" zien en de matchup een tweede keer resolven — met dubbele MMR/beloning tot gevolg. Opgelost met een `asyncio.Lock` (`self._resolve_lock`) om de hele resolutie+voortgang-sectie, plus een `self._afgerond`-vlag die een matchup na afloop van het gevecht blokkeert voor herverwerking (nodig omdat de laatste ronde de tactieken bewust niet reset). Dezelfde vlag beschermt ook `_wegrennen`. **Wegrennen geeft niet langer een kunstmatige 3-0/0-3-stand**: de echte, tot dan toe behaalde score blijft gewoon staan; wie wint bij een vlucht wordt nu direct via `gevlucht_id` bepaald in `_verwerk_einde`, niet via de (soms tegen de vluchter gunstige) tussenstand. **`UitdagingView` stopt zichzelf na acceptatie/weigering** (`self.stop()` + een `_verwerkt`-vlag) — anders bleef de timeout (180s) actief en logde die alsnog een misleidende "verliep zonder reactie"-melding voor een allang afgehandelde uitdaging (exact zo gemeld: 2 losse timeout-logs voor 1 gevecht dat al was gespeeld). Getest: een geforceerde race (twee taken die interleaven via `asyncio.gather`) blijft op een totaal van max. 3 matchups staan; wegrennen na een 1-0 tussenstand levert een eindstand van 1-0 op (niet 1-3); een dubbelklik op Accepteren start het gevecht maar 1x; `on_timeout` na verwerking doet niets meer. **VS-badge vervangen door gekruiste zwaarden** (2026-07-26, verzoek van de gebruiker: de rode bol met "VS"-tekst mocht weg): `utils/vecht_afbeelding.py:_teken_kruisende_zwaarden` tekent twee zelfgetekende zwaarden in een X (kling/greep/garde/pommel als simpele vormen, geen emoji-lettertype nodig — dat ontbreekt soms op de Linux-server waar de bot draait). **Vriendschappelijke modus toegevoegd (2026-07-26, verzoek van de gebruiker: een gevechtsmodus die altijd beschikbaar is en niet van de dagelijkse ranked-limiet afgaat)**: `/vecht` kreeg een `modus`-parameter (`ranked`/`vriendschappelijk`, standaard `ranked`). Vriendschappelijk: slaat de dagelijkse ranked-poging-check/verbruik helemaal over (werkt dus ook als je gratis pogingen + tokens op zijn), geeft geen MMR-verandering, geen Chaos Coins-beloning, geen XP voor pets, en geen blessure voor de verliezende pet; ook geen inzet mogelijk (bij PvP wordt het `VechtInzetView`-paneel overgeslagen, de uitdaging gaat direct de deur uit). Verder identieke flow (tactieken, elementen, VS-afbeelding, PvE/PvP). Doorgegeven via een nieuwe `is_friendly`-vlag op `UitdagingView` en `VechtView`. Getest via `scripts/test_vecht_friendly.py`: zowel PvE als PvP vriendschappelijk werkt met kunstmatig uitgeputte ranked-pogingen (999), en MMR/coins/ranked-pogingen/blessures blijven aantoonbaar ongewijzigd na afloop.
- **PvE-tegenstander schaalt per matchup op de eigen pet-macht** (2026-07-22, i.p.v. alleen op MMR): MMR beweegt nauwelijks (+16/winst) terwijl een enkele Legendary-vangst de teammacht kan verdubbelen, dus een MMR-only tegenstander werd al snel makkelijk te verslaan (gemeld door de gebruiker met bewijs: 367 eigen macht vs 210 tegenstander-macht bij mmr 1048). Eerste poging (schalen op teamtotaal) was zelf ook fout: bij een scheef verdeeld team (1 sterke pet + 2 zwakke) verloor je alsnog 2 van de 3 matchups ondanks een gelijk totaal, omdat matchups per pet zijn, niet team-totaal-vs-totaal. Juiste fix: elke matchup spiegelt de tegenstander-macht op de macht van de eigen pet in díé matchup specifiek, met een kleine MMR-modifier erbovenop. Getest: ~50/50 winstkans tegen een even sterke tegenstander, ook bij een scheef team.
- **Trading** (sectie 11, 2026-07-24): `/trade speler` — direct ruilen tussen twee spelers. **UI herbouwd naar een paneel (2026-07-24, feedback van de gebruiker: losse command-parameters waren verwarrend)**: `/trade speler` neemt alleen de tegenspeler als parameter; de rest wordt samengesteld via `TradeBuilderView` (ephemeral, alleen zichtbaar voor de voorsteller) — twee dropdowns ("Wat bied jij aan?" / "Wat vraag je terug?"), gevuld met de daadwerkelijke items (met voorraad) en niet-werkende pets van resp. jezelf en de andere speler, plus per kant losse knoppen voor aantal (`AantalModal`) en Chaos Coins (`CoinsModal`) — bewust twee losse modals i.p.v. één gecombineerde (2026-07-24, feedback van de gebruiker: "Aanbod: aantal/coins" in één knop was vaag). Elke kant blijft óf één item-stack óf één pet, plus optioneel coins (marktplaats-verkoop via `/verkoop` is bewust geschrapt, dat wordt door `/release` afgedekt). Bij "Versturen" gaat het pas als publiek bericht de deur uit met de bestaande twee-staps bevestiging: de ontvanger accepteert/weigert eerst (`TradeVoorstelView`), en pas daarna moet de voorsteller nóg een keer definitief bevestigen (`TradeBevestigView`) — extra zekerheid tegen typefouten. Beide bezitscontroles (`_bezit_kant`) draaien opnieuw vlak vóór de daadwerkelijke overdracht, voor het geval iemands inventaris/pets tussentijds veranderd zijn. Een verhandelde pet krijgt een nieuw volgnummer bij de nieuwe eigenaar (uit diens `Speler.volgend_pet_nummer`-teller, net als bij vangen) en de status wordt gereset naar `rust`. Pets die aan het werk zijn (`status=werkplek`) kunnen niet verhandeld worden (en verschijnen niet in de dropdown). Eigen logcategorie `trade`, toegevoegd aan de `/setlog`-dropdown. `TradeKant` toont nu ook de pet-naam (5e veld, puur weergave) i.p.v. alleen het nummer. **Samengestelde ruil-afbeelding (2026-07-24, verzoek van de gebruiker)**: als beide kanten van een voorstel een pet zijn, rendert `utils/trade_image.py` (nieuwe Pillow-dependency) de twee pet-afbeeldingen naast elkaar met een blauwe dubbele ruilpijl ertussen, als PNG-bijlage via `discord.File` + `embed.set_image(url="attachment://ruil.png")` — Discord-embeds ondersteunen zelf maar één losse afbeelding, dus dit wordt zelf samengesteld i.p.v. twee losse image-velden. Valt stil terug op geen afbeelding als een download mislukt (bijv. netwerkfout) of een van beide kanten geen pet is. Getest via `scripts/test_trading.py`, inclusief het hele paneel (dropdown-selectie + modal-invoer + versturen) en een losse check dat een pet-voor-pet ruil een `file` meestuurt.
- **`/release`** (2026-07-24): laat een pet vrij tegen Chaos Coins (`_release_beloning`: zelfde groeipatroon als `pet_power` — `basis x tier_multiplier x (1 + level x 0,05)`, basis 15) + 15% kans op een bonus-grondstof (bij voorkeur die van de werkplek-voorkeur van de soort, anders een willekeurige grondstof). Eén waarschuwings-embed met Bevestigen/Annuleren-knoppen (geen tegenpartij zoals bij trading, dus geen tweede bevestigingsronde nodig). Pets die aan het werk zijn kunnen niet vrijgelaten worden. Logt naar de `vangst`-categorie (2026-07-24, verzoek van de gebruiker: release-logs bij vangst-logs, want thematisch elkaars tegenpool), niet naar `main`. Getest via `scripts/test_trading.py` (bevestigen verwijdert de pet en kent de juiste coins toe, werkende pet wordt geweigerd).
- **Elementen & contra's** (2026-07-26, backlog-item, zie verzoek hieronder bij de historie): elk van de 125 pet-soorten heeft nu een `element` (`db/models.py:Element` — grond/water/lucht/vuur/chaos, migratie `0326f4a6c27c`), zelf toegewezen op thema in `scripts/seed.py:ELEMENT_MAP` (50 grond, 26 water, 19 lucht, 12 vuur, 18 chaos — alle "Chaos X"-soorten zijn element chaos). Contra-cirkel **Vuur > Lucht > Grond > Water > Vuur** in `utils/elementen.py`: een gunstige matchup geeft +15% macht, een ongunstige -10%, twee stappen verschil is neutraal (1.0x) — bewust milder dan het eerste concept (+20%/-15%) op verzoek van de gebruiker. **Chaos** aan een van beide kanten maakt de uitkomst willekeurig (+15%/-10%/neutraal), past bij de al bestaande "wisselt dagelijks"-flavor. Toegepast in `cogs/gevechten.py:VechtView._kies_tactiek` als extra multiplier op de macht van beide kanten vóórdat `speel_matchup` de tactiek-variantie toepast. **PvE-tegenstander is niet langer naamloos** (2026-07-26, verzoek van de gebruiker: eerlijker als je vooraf weet tegen wat/welk element je vecht): per matchup wordt via `_kies_wilde_tegenstanders()` een willekeurige, unieke soort gekozen uit `WILDE_NAMEN` (30 bestaande soorten verspreid over tiers/families, bijv. Wolf/Krokodil/Steenarend/Bizon), en die soort levert zijn **echte** naam, element én afbeelding — niet meer een generieke "gesimuleerde tegenstander" met een willekeurig element. Element zichtbaar via emoji (⛰️🌊🌪️🔥🌀) bij `/lijst` (per pet), `/team` (dropdown + samenvatting), en in de matchup-titel tijdens `/vecht` (bijv. "⛰️ Egel vs 🌊 Wilde Walrus"). **Samengestelde VS-afbeelding tijdens gevechten** (verzoek van de gebruiker, zelfde patroon als de trade-afbeelding): `utils/vecht_afbeelding.py` rendert de twee pets van de huidige matchup naast elkaar met een rode "VS"-badge, als bijlage bij elke matchup-intro — nu ook in PvE, omdat de wilde tegenstander een echte afbeelding heeft. De download/bijsnijd-logica is gedeeld met trading via `utils/afbeeldingen.py` (`download_afbeelding`, `kwadraat_bijsnijden`, `soort_afbeeldingen`). Getest via `scripts/test_elementen.py` (contra-cirkel-logica, chaos-willekeur, en een volledig gesimuleerd PvE-gevecht end-to-end) en handmatige checks dat de matchup-titel en VS-afbeelding in PvE de echte wilde soort tonen.
- **Critterdex + `/info`, afgerond (2026-07-28, verzoek van de gebruiker)**: nieuwe cog `cogs/critterdex.py`. **`/critterdex`** — gepagineerd (10/pagina, net als `/lijst`) overzicht van alle 150 pet-soorten, standaard gesorteerd op tier + alfabet, met per soort tier-emoji, element-emoji, naam, en of/hoe vaak je 'm zelf al gevangen hebt ("Gevangen: Nx" of "Nog niet gevangen"). Twee dropdown-filters (`CritterdexView`, rij 0/1): tier (Alle/Common/.../Legendary) en element (Alle/Grond/Water/Lucht/Vuur/Chaos), te combineren; paginering-knoppen op rij 2. **Bewuste keuze (afgestemd met de gebruiker)**: nog niet gevangen soorten tonen gewoon hun volledige info — geen Pokédex-stijl "???"-verberging, consistent met de rest van de bot (nergens anders wordt info voor spelers verborgen) en simpeler te bouwen. **`/info <soort>`** — losse lookup (autocomplete op naam): tier, element, gevecht/werk-stats als kwalitatief label (`_stat_label()`, dezelfde ZEER_LAAG..HOOGSTE-schaal als `scripts/seed.py`, Laag/Gemiddeld/Hoog/Zeer hoog/Hoogste), werkplek-voorkeur, flavor-tekst, en je eigen vangst-aantal. `/critterdex` is publiek zichtbaar (zelfde als `/lijst`); `/info` is ephemeral (zelfde als `/verzorg`'s stats-weergave, een persoonlijke losse lookup). Getest via nieuw `scripts/test_critterdex.py`: `_stat_label`-grenzen, paginering (150 soorten, juist aantal per pagina), tier-filter + element-filter (los en gecombineerd), "gevangen"-status klopt na het vangen van een testpet, en `/info` toont de juiste velden inclusief nette foutmelding bij een onbekende soortnaam. Regressie gecheckt met alle bestaande testscripts. **Vinkje/kruisje toegevoegd (2026-07-28, verzoek van de gebruiker: "maakt het net wat duidelijker")**: ✅/❌ vóór de gevangen-status in zowel `/critterdex` als `/info`'s "Zelf gevangen"-veld, i.p.v. alleen tekst.
- **Mini-wiki, afgerond (2026-07-28, verzoek van de gebruiker)**: nieuwe cog `cogs/wiki.py`, commando `/wiki` (publiek, zoals `/lijst`). **Scope-keuze (afgestemd met de gebruiker)**: combineert uitleg van de spelmechaniek MET de bijbehorende commando's per onderwerp — een aanvulling op `/commands` (dat alleen command-syntax + 1 zin toont) i.p.v. een duplicaat ervan. **8 onderwerpen** (`WIKI_ONDERWERPEN`): Vangen & Tiers, Elementen & contra's, Verzorgen, Werken & grondstoffen, Vechten & ranked, Traden & releasen, Clans, Leveling — elk met een prozastukje over de mechaniek (spawnkansen, de contra-cirkel, hoe voerbakken nu echt voeren, werkplek-capaciteit per clan, Elo/tactieken, enz.) plus een lijst relevante commando's. **UI (`WikiView`)**: een dropdown om direct naar een onderwerp te springen (zoals de backlog vroeg), plus Vorige/Volgende-knoppen — die navigeren hier bewust **tussen onderwerpen** (i.p.v. binnen 1 onderwerp te pagineren), omdat elk onderwerp in één beknopt embed past en er dus niets binnen een onderwerp te pagineren valt; dit geeft nog steeds hetzelfde "blader-gevoel" als `/lijst`. Getest via nieuw `scripts/test_wiki.py`: alle onderwerpen passen binnen Discord's embed-/veldlimieten, Vorige/Volgende doorlopen alle onderwerpen met correcte disabled-status aan de uiteinden, de dropdown springt direct naar de juiste index, en `interaction_check` laat alleen de eigenaar van het paneel toe. Regressie gecheckt met de volledige testsuite. **Inhoudelijke review + correcties (2026-07-28, tweede sessie op verzoek van de gebruiker: alle wiki-teksten regel voor regel geverifieerd tegen de daadwerkelijke implementatie)** — vier feitelijke fouten gevonden en gecorrigeerd: (1) Traden claimde "items en/of een pet van elke kant", maar `cogs/trading.py:TradeKant` documenteert expliciet dat item en pet **onderling exclusief** zijn; (2) Vechten suggereerde dat verliezen alleen straf oplevert, terwijl `XP_VERLIES = 10` betekent dat het hele team óók bij verlies XP krijgt (30 bij winst); (3) Vechten zei "de verslagen pet" krijgt een blessure, maar `_blesseer()` wordt per verloren **matchup** aangeroepen — je kan een gevecht winnen en tóch een geblesseerde pet overhouden; (4) Vangen & Tiers claimde dat een hogere tier "hogere basisstats" geeft, maar `stat_multiplier` wordt alleen gebruikt in `pet_power()` en `_release_beloning()` — de **werk-opbrengst gebruikt uitsluitend `werk_genen`**, dus tier heeft daar géén invloed. Daarnaast aangevuld met spelrelevante info die ontbrak: de inzetbaarheidsdrempels (energie < 20 / honger 0 blokkeren `/werk` en `/team` — de meest voorkomende reden dat een commando weigert), de max-2-pets-tegelijk-aan-het-werk-limiet, werkplek-capaciteiten, de 3 gratis ranked-pogingen + Extra match token, de PvP-inzetmechaniek (die volledig ontbrak), en dat level bij gevechten dubbel meetelt (genen-groei plus een losse machtsbonus in `pet_power`, waardoor het levelverschil in de arena veel groter uitpakt dan op de werkplek). **Vechten opgesplitst in twee onderwerpen** ("⚔️ Vechten & ranked" + "🤝 PvP & inzet") zodat geen enkel blok onevenredig lang werd — de wiki telt nu **9 onderwerpen**. Detailniveau bewust beperkt tot harde limieten en niet-raadbare getallen (afgestemd met de gebruiker): geen exacte energie-kosten per shift, output-per-uur of XP-formules, want die veranderen bij elke balans-tweak en zouden de wiki snel onjuist maken.
- **Web-adminpanel fase 1, afgerond (2026-07-29, verzoek van de gebruiker: "zo compleet en makkelijk mogelijk")**: nieuwe map `portal/` met een aiohttp-server die **in hetzelfde proces en dezelfde event loop als de bot** draait, plus `web/admin.html` als paneel. **Setup-instructies staan apart in `docs/admin-portal-setup.md`** (Discord OAuth, env-vars, Railway-domein) — die moet de gebruiker zelf uitvoeren; zolang de env-vars ontbreken start de portal niet en logt de bot alleen een waarschuwing, de bot zelf blijft werken. **Hosting-keuze (afgestemd)**: de bot serveert het paneel zelf op `/`, met `critters.casualchaos.nl` als custom subdomein op Railway (CNAME) — dus geen aparte hosting, geen handmatig kopiëren van HTML naar de website, en géén CORS-configuratie. Een *pad* als `casualchaos.nl/critters-admin` kan niet met DNS alleen (dat zou een reverse proxy vereisen), vandaar een subdomein. **Stijl** volledig overgenomen van het bestaande Casual Chaos-paneel (dezelfde CSS-variabelen, Rajdhani/Inter, sidebar met hamburger onder 860px, `.card`/`.field`/`.btn`-componenten). **Auth**: Discord OAuth met alleen de `identify`-scope; de adminrechten worden daarna **via de bot zelf** gecontroleerd (`guild.fetch_member` op `ADMIN_GUILD_ID` + de nieuwe `utils/checks.py:member_is_admin`, losgetrokken uit `is_admin` zodat de portal en de Discord-commando's dezelfde bron van waarheid hebben). `fetch_member` doet een API-call en werkt dus ook zonder de members-intent, die deze bot niet aan heeft. OAuth-`state` als CSRF-bescherming (eenmalig verzilverbaar, 10 min geldig). Sessies staan in een nieuwe tabel `portal_sessies` (migratie `a7d41e8b3f52`) i.p.v. in het geheugen: Railway herstart bij elke deploy, en in-memory zou betekenen dat je daarbij steeds uitgelogd wordt. **Wat instelbaar is (fase 1 = alles wat al in de database stond, dus nul risico voor de spellogica)**: de 5 `Instelling`-waarden, item-prijzen+beschrijvingen, werkplekken (output/capaciteit/beide grondstoffen/bonus-kans), tiers (spawnkans+multiplier, met waarschuwing als het totaal geen 100% is), **volledig pet-soortenbeheer incl. toevoegen/verwijderen**, spelerbeheer (coins/MMR/ranked-pogingen, items geven+afnemen, pets bewerken, blessure opheffen, van het werk halen, pet verwijderen), clans ontbinden, spawn- en logkanalen, en een statistiek-overzicht. **Bewust read-only gehouden**: item-namen en -types (alle 16 komen als string terug in `RECEPT_KOSTEN`, `HONGER_HERSTEL_WAARDEN`, `VOERBAK_ITEMS_PER_NIVEAU`, `VOERBAK_NIVEAUS` en de Extra match token-lookup — hernoemen zou stille bugs geven) en werkplek-`type` (de `/werk`-keuzelijst is een hardcoded `app_commands.Choice`-lijst, dus een nieuwe werkplek zou onbereikbaar zijn). Om diezelfde reden kan je items en werkplekken niet toevoegen/verwijderen; dat staat als waarschuwing in het paneel zelf. **Twee dingen die vanuit de portal actief gesynchroniseerd worden**: de in-memory spawn-kanaal-cache in `cogs/vangen.py` (zonder dat zou een nieuw spawn-kanaal pas na een herstart werken) en `sync_stats_met_voerbak()` vóór het tonen van pet-stats (honger/energie vervallen lazy, dus anders zou het paneel verouderde waarden laten zien). **Pet-soort verwijderen is geblokkeerd** zolang spelers exemplaren hebben (FK naar een verdwenen soort zou `/lijst` laten crashen). **XSS-valkuil onderweg gevonden en gefixt**: de eerste versie interpoleerde namen in `onclick`-attributen. Browsers HTML-decoderen zo'n attribuut vóórdat het als JS geparsed wordt, dus een ge-escapete apostrof wordt daar weer een echte quote — en pet-namen zijn spelerbepaald (Naamkaartje), dus een pet met de naam `');alert(1);//` had echt code kunnen uitvoeren in het adminpaneel. Nu worden alleen numerieke ID's geïnterpoleerd en namen uit de geladen data opgezocht; `esc()` heeft een comment dat het niet veilig is voor JS-in-attributen. Getest via nieuw `scripts/test_portal.py`: draait de echte app via aiohttp's `TestServer` met een nep-bot en een zelf aangemaakte sessie, en controleert dat `/api/*` zonder (of met een onbekend) token 401 geeft terwijl het loginscherm en `/health` open zijn, dat instellingen/werkplekken/tiers/soorten/spelers/pets/items/kanalen allemaal werken, dat onzin-invoer 400 geeft (capaciteit 0, kans 500%, negatieve output, spawnkans 200%, lege naam, honger 500, meer items afnemen dan iemand heeft), dat de spawn-kanaal-cache echt bijgewerkt wordt, en dat soort-verwijderen geblokkeerd is bij een gevangen exemplaar. Volledige regressiesuite gedraaid (14 scripts, allemaal groen).

## Nog niet gebouwd (herprioriteerd door de gebruiker, 2026-07-22; item 9 toegevoegd 2026-07-24; Trading, /release, Elementen & contra's, Werkplekken uitbreiden, Item-overhaul (beide delen), Clan-systeem, Volledige balans-audit, Critterdex, Mini-wiki, Web-adminpanel fase 1 gebouwd; Fokken naar achteraan verplaatst 2026-07-26)

Het verzorgingssysteem, het level-up systeem, team & gevechten, trading, /release, elementen & contra's, werkplekken uitbreiden, de volledige item-overhaul, het clan-systeem, de volledige balans-audit, de Critterdex, de Mini-wiki, en fase 1 van het web-adminpanel zijn af, zie "Wat werkt" en "Balans-audit resultaat" hieronder.

**Doorlopend, geen afvinkbaar punt**: nieuwe pet-soorten blijven erbij komen naast het onderstaande werk — dat is nooit "klaar".

1. ~~**Admin panel fase 2: balansconstanten naar de database**~~ **Afgerond op 2026-07-30** (blok 1 t/m 5, zie de blok-secties verderop). Alle balansconstanten uit de inventaris staan nu in de database en zijn via de portal aanpasbaar zonder deploy. De inventaris onder "Voorbereiding volgende sessie" blijft staan als historie/naslag, maar is geen openstaand werk meer.
2. **Fokken/breeding** (`cogs/fokken.py` is placeholder) — **het enige nog openstaande punt.** Lange termijn, verzet naar achteraan (2026-07-26, verzoek van de gebruiker: pas echt interessant vanaf 250 pet-soorten; nu 150). Ontwerp staat in projectbrief sectie 10: zelfde soort = ~80% kans op die soort, verschillende soorten = 15-20% kans op een hybride, genen zijn het gemiddelde van de ouders plus variatie, kost currency/grondstoffen, cooldown per pet.

## Voorbereiding volgende sessie: Admin panel fase 2 (2026-07-29)

**Fase 1 is af en staat op `main` én `dev`** (laatste commit `fbeda4c`). Alle 14 testscripts slagen. De volgende sessie draait op een andere machine: begin met `git pull` op de juiste branch.

### Stand van zaken van de portal zelf

**Bevestigd werkend (2026-07-30, gebruiker heeft ingelogd en het paneel gecheckt)** — het TLS-certificaat is inmiddels uitgegeven, `https://critters.casualchaos.nl` werkt end-to-end. Fase 1 is daarmee volledig afgerond, niets meer open op dit punt.

### Wat fase 2 inhoudt

De balanswaarden die nog **hardcoded Python-constanten** zijn, naar de database verhuizen zodat de portal ze kan aanpassen. Onderstaande inventaris is geverifieerd op 2026-07-29 (bestand + regelnummer + huidige waarde), dus die hoeft niet opnieuw uitgezocht te worden.

**Losse getallen — 27 stuks, direct geschikt voor de bestaande key/value `Instelling`-tabel:**

| Bestand | Constanten |
|---|---|
| `utils/leveling.py` | `MAX_LEVEL`=50 (r9), `GENEN_GROEI_PER_LEVEL`=0.02 (r10), `XP_PER_EFFECTIEVE_UUR`=180 (r24), plus de factor 100 die hardcoded in `xp_voor_volgend_level()` staat (r27) |
| `cogs/werk.py` | `CURRENCY_PER_GRONDSTOF`=2 (r22), `BONUS_GRONDSTOF_AANTAL`=1 (r23) |
| `utils/stats.py` | `_HONGER_VERVAL_MINUTEN_ECHT`=20 (r21), `_ENERGIE_HERSTEL_MINUTEN_ECHT`=10 (r22), `_DEV_VERSNELLING`=120 (r25), `ENERGIE_MINIMUM`=20 (r34), `_SLAAP_COOLDOWN_UUR_ECHT`=24 (r66), `SLAAP_HONGER_KOST`=20 (r70), `_BLESSURE_DUUR_UUR_ECHT`=2 (r72) |
| `utils/gevechten.py` | `MAX_INTERNE_RONDES`=5 (r32), `SCHADE_FRACTIE`=0.35 (r33), `ELO_K`=32 (r35), `CURRENCY_BASIS_WINST`=20 (r36), `CURRENCY_BONUS_PER_100_MMR`=2 (r37), `XP_WINST`=30 (r38), `XP_VERLIES`=10 (r39), `ENERGIE_KOST_MIN`=10 (r40), `ENERGIE_KOST_MAX`=20 (r41), `_RANKED_RESET_UUR_ECHT`=24 (r43) |
| `utils/elementen.py` | `BONUS`=1.15 (r28), `MALUS`=0.90 (r29) |
| `cogs/release.py` | `RELEASE_BASIS_COINS`=15 (r16), `BONUS_ITEM_KANS`=0.15 (r17) |

**Gestructureerde data — ~40 losse getallen, past níét in platte key/value:**

| Wat | Waar | Vorm |
|---|---|---|
| `WERK_CYCLI` (3 cycli x duur/energie/multiplier = 9) | `cogs/werk.py` r38-41 | dict van frozen dataclasses, opgebouwd op **import-tijd** |
| `TACTIEK_VARIANTIE` (3 tactieken x min/max = 6) | `utils/gevechten.py` r26 | dict van tuples |
| `RECEPT_KOSTEN` (10 recepten x 2 ingredienten = 20) | `cogs/verzorging.py` r71 | dict van lijsten van tuples, met itemnamen als string |
| Voer-effecten (5) | `utils/stats.py` r56-64 | `HONGER_HERSTEL_WAARDEN`, `VOLLEDIG_HERSTEL_ITEMS`, `VOERBAK_ITEMS_PER_NIVEAU` — allemaal op itemnaam |

### Aanpak-advies

**Niet** elke call-site een async DB-read laten doen: deze waarden worden in loops gelezen (bijv. per gevechtsronde), dat wordt traag en invasief. Beter een cache-module, bijvoorbeeld `utils/balans.py`:

- laadt bij het opstarten van de bot alle rijen uit `Instelling` in een dict;
- biedt synchrone getters met een default: `balans.get_int("xp_per_effectieve_uur", 180)`, `get_float(...)`;
- wordt door de portal geinvalideerd/herladen zodra daar iets opgeslagen wordt (de portal draait in hetzelfde proces, dus dat is een directe functieaanroep — geen polling nodig).

Call-sites veranderen dan van `XP_PER_EFFECTIEVE_UUR` naar `balans.get_int(...)`, wat een kleine, mechanische wijziging is.

**Drie valkuilen die tijd gaan kosten:**

1. **De dev-versnelling wordt op import-tijd berekend.** `HONGER_VERVAL_MINUTEN`, `ENERGIE_HERSTEL_MINUTEN`, `SLAAP_COOLDOWN_UUR`, `BLESSURE_DUUR_UUR` (`utils/stats.py`) en `RANKED_RESET_UUR` (`utils/gevechten.py`) zijn afgeleide module-constanten met `config.ENVIRONMENT` er al in verwerkt. Die moeten functies worden, anders bevriest de oude waarde bij import.
2. **`WERK_CYCLI` is het lastigste geval.** Dict van `frozen=True` dataclasses, opgebouwd op import-tijd, en op meerdere plekken geimporteerd (`cogs/werk.py`, `cogs/verzorging.py`, `scripts/`). Wordt een functie `werk_cycli()` die de dataclasses per aanroep opbouwt uit de cache.
3. **De gestructureerde data verdient eigen tabellen, geen JSON-in-een-string.** `RECEPT_KOSTEN` is in feite al een relatie (item -> benodigde items met aantal); een `recepten`-tabel met FK's naar `items` is netter dan JSON, en de portal kan er dan een fatsoenlijke editor voor krijgen met dropdowns i.p.v. een tekstveld. Zelfde voor de werk-cycli.

### Volgorde-advies

Per blok bouwen en na elk blok testen, niet alles in een keer. Voorstel:

1. `utils/balans.py` + de cache-invalidatie vanuit de portal + een portal-tab die de losse getallen toont. Begin met een klein, ongevaarlijk blok (bijv. `utils/elementen.py`: maar 2 waarden) om de hele keten te bewijzen.
2. De overige losse getallen per bestand (leveling, release, gevechten, stats, werk).
3. `WERK_CYCLI` (aparte tabel + portal-editor).
4. `RECEPT_KOSTEN` (aparte tabel met FK's + portal-editor).
5. De voer-effecten en `TACTIEK_VARIANTIE`.

**Na elk blok**: het bijbehorende testscript draaien, en aan het eind de volledige suite. Let vooral op `test_leveling.py`, `test_werk.py`, `test_elementen.py`, `test_vecht_friendly.py` en `test_grondstof_recepten.py` — die lezen de constanten die je aanpast.

De volledige suite (14 scripts, allemaal groen op 2026-07-29) — `test_vangen.py` staat er bewust niet in, die is stuk:

```bash
for f in test_clan test_craft test_critterdex test_elementen test_grondstof_recepten \
         test_item_overhaul test_leveling test_portal test_pvp_namen test_trading \
         test_vecht_friendly test_verzorging test_werk test_wiki; do
  python3 scripts/$f.py >/dev/null 2>&1 && echo "PASS  $f" || echo "FAIL  $f"
done
```

Ze draaien tegen een echte database, dus `DATABASE_URL` moet gezet zijn (de dev-database — níét de prod-database, de scripts maken en verwijderen testdata).

### Losse eindjes die blijven staan

- **`scripts/test_vangen.py` is stuk** (`VangenCog._vind_soort` bestaat niet meer, hernoemd tijdens eerdere refactors). Nog steeds niet gefixt, bewust buiten scope. Dit is het enige testscript dat niet meedraait in de suite.
- **`/give` en `/herstel`** zijn door de portal grotendeels overbodig geworden. Nog niet verwijderd: eerst kijken of het paneel in de praktijk fijner werkt.
- **Credentials, opgelost (2026-07-30)**: zowel de Discord client secret als de twee database-wachtwoorden (dev + prod) — die tijdens de vorige sessie in de chat hadden gestaan — zijn inmiddels geroteerd door de gebruiker.

## Balans-audit resultaat (2026-07-28)

Alle 7 audit-vragen zijn met de gebruiker doorgenomen en doorgevoerd (code + 1 nieuwe migratie `2b8a6f31c9de`, toegepast op de live DB). Alle testscripts in `scripts/` zijn bijgewerkt en slagen (zie hieronder).

1. **XP-tempo, opgelost**: doel was ~2-4 weken tot level 50 (was ~227 dagen). `utils/leveling.py:XP_PER_EFFECTIEVE_UUR` 5 → **95**. ⚠️ **De schatting van "~21 dagen" hierbij was fout** — zie de correctie onder "Balans-hercontrole" hieronder; 95 kwam in werkelijkheid uit op ~45 dagen en is later bijgesteld naar 180.
2. **Receptkosten, fors omhoog + multi-ingredient**: eerste voorstel (3-4x hoger) bleek bij doorrekenen nog steeds triviaal (1 lange shift dekte een heel recept) — hoofdgrondstoffen leverden simpelweg te veel per shift op. Definitieve `RECEPT_KOSTEN` (`cogs/verzorging.py`), nu allemaal met 2 ingrediënten uit 2 verschillende werkplekken (breed toegepast, verzoek van de gebruiker, niet alleen op de 2 "OP"-items):
   - Graanvrije premium voeding: Groente x12 + Water x1
   - Vers vlees/vis: Algen x15 + Takken x8
   - Mysterie voedselzak: Fruit x1 + Bladeren x1
   - Naamkaartje: Takken x15 + Spijker x1
   - Focus drankje: Bladeren x2 + Edelsteen x1
   - Werk-elixer: Erts x12 + Spijker x2 (was al 2 ingrediënten, Erts omhoog)
   - Extra match token: Maanschijnkristal x30 + Edelsteen x2 (was al 2 ingrediënten, beide omhoog)
   - Simpele voerbak: Water x2 + Fruit x2
   - Slimme voerbak: Schroot x40 + Erts x20 (was alleen Schroot x5)
   - Zelfreinigend systeem: Sterrenstof x3 + Schroot x20 (was alleen Sterrenstof x2)

   Basis brokjes blijft bewust zonder recept (enige "gratis" fallback-voeding als je geen grondstoffen hebt). Bonus-grondstoffen (Water/Sterrenstof/Fruit/Bladeren/Spijker/Edelsteen, 25%-kans per shift) bleven bewust klein — die zijn door hun zeldzaamheid al een serieuze tijdsinvestering; hoofdgrondstoffen zijn de knop die echt omhoog moest.
3. **`/shop`-UX**: gebruiker koos voor een apart `/craft`-commando. **Afgerond (2026-07-28)**, zie "Wat werkt" hieronder.
4. **Item-prijzen (Chaos Coins)**: ongewijzigd gelaten. Berekening: een overnacht-shift op een gemiddelde werkplek levert nu ~150-200 Chaos Coins op (1 pet, 1x per dag) — de bestaande prijzen (10-300) blijven daarmee prima getrapt; de receptkosten (punt 2) zijn de eigenlijke grindrem geworden.
5. **Shift-efficiëntie gelijkgetrokken**: `cogs/werk.py`, `output_multiplier` voor korte/lange/overnacht ging van **1.0 / 2.8 / 4.5** naar **2.0 / 2.3 / 2.6** — overnacht was 4,5x zo efficiënt per uur als korte, nu nog maar 1,3x. Korte/lange shifts zijn hierdoor fors aantrekkelijker geworden; overnacht behoudt een lichte voorkeurspositie (minder inlogmomenten nodig). `energie_kost` (20/50/70) bewust ongewijzigd gelaten (niet expliciet gevraagd, en het overblijvende voordeel van overnacht op die dimensie is een logisch/gewenst neveneffect van een vlakke kost-per-shift-start).
6. **Werkplek-capaciteit + bonus-kans**: Nachtwacht-capaciteit 1 → **2** (was de enige werkplek met capaciteit 1, en sinds de ranked-lek-fix ook nodig voor Maanschijnkristal — onbedoeld knelpunt). Bonus-kans (25%) ongewijzigd, geen concreet probleem gevonden.
7. **Gevecht-economie**: ongewijzigd, puur voor de zekerheid gecheckt, geen concreet probleem gevonden.

**Ook aangepast n.a.v. de berekeningen tijdens de audit**: `max_werkende_pets_per_speler` 3 → **2** (DB-backed, `Instelling`-tabel) — bij 3 kon 1 speler in z'n eentje de hele Moestuin-werkplek (capaciteit 3) bezet houden; bij 2 kan dat nergens meer volledig.

**Tijdens het bijwerken van testscripts gevonden, niet gerelateerd aan de audit**: `scripts/test_leveling.py` en `scripts/test_verzorging.py` misten `volgnummer` bij het direct aanmaken van een `Huisdier` (stamde van vóór de per-speler-pet-nummers-feature, 2026-07-22) — gefixt, meegenomen omdat het toevallig dezelfde testscripts raakte. **`scripts/test_vangen.py` is stukgevallen** (`VangenCog._vind_soort` bestaat niet meer, waarschijnlijk hernoemd/verplaatst tijdens latere refactors) — **niet gefixt**, buiten scope van deze sessie, los oppakken.

## Balans-audit referentie (bijgewerkt 2026-07-28)

**Alle placeholder-balanswaarden, met exacte locatie en huidige waarde na de audit**:
- `cogs/werk.py`: `CURRENCY_PER_GRONDSTOF` = 2, `BONUS_GRONDSTOF_AANTAL` = 1, cyclus `energie_kost` (20/50/70, ongewijzigd) en `output_multiplier` (**2.0/2.3/2.6**, was 1.0/2.8/4.5) voor korte/lange/overnacht.
- `scripts/seed.py`: stat-schaal `ZEER_LAAG..HOOGSTE` = 10/20/40/60/80/95; `WERKPLEKKEN[].output_per_uur` (Moestuin 5.0, Vijver 6.0, Werkbank 6.0, Bos 5.5, Nachtwacht 7.0, Mijnschacht 6.5) en `.capaciteit` (3/2/2/2/**2**/2, Nachtwacht was 1); `TIERS[].spawnkans` (45/25/18/9/3%) en `.stat_multiplier` (1.0/1.2/1.4/1.7/2.0); alle `ITEMS[].prijs` (ongewijzigd).
- `db/models.py` / migratie `86eb7fa1f495`: `Werkplek.opbrengst_2_kans` default 0.25 voor iedere werkplek (ongewijzigd).
- `utils/leveling.py`: `XP_PER_EFFECTIEVE_UUR` = **180** (was 5, tussentijds 95 — zie "Balans-hercontrole"), `MAX_LEVEL` = 50, `GENEN_GROEI_PER_LEVEL` = 0.02, level-curve `huidig_level * 100` (ongewijzigd).
- `cogs/verzorging.py`: `RECEPT_KOSTEN` — zie "Balans-audit resultaat" hierboven voor de volledige, herziene tabel.
- `utils/stats.py`: `_HONGER_VERVAL_MINUTEN_ECHT` = 20, `_ENERGIE_HERSTEL_MINUTEN_ECHT` = 10, `ENERGIE_MINIMUM` = 20, `SIMPELE_VOERBAK_FACTOR` = 2, `SLAAP_HONGER_KOST` = 20, `_SLAAP_COOLDOWN_UUR_ECHT` = 24, `_BLESSURE_DUUR_UUR_ECHT` = 2 (allemaal ongewijzigd).
- `cogs/release.py`: `RELEASE_BASIS_COINS` = 15, `BONUS_ITEM_KANS` = 0.15 (ongewijzigd).
- `utils/elementen.py`: `BONUS` = 1.15, `MALUS` = 0.90 (ongewijzigd).
- `utils/gevechten.py`: `ELO_K` = 32, `CURRENCY_BASIS_WINST` = 20, `CURRENCY_BONUS_PER_100_MMR` = 2, `XP_WINST`/`XP_VERLIES` = 30/10, `ENERGIE_KOST_MIN`/`MAX` = 10/20, `TACTIEK_VARIANTIE` (aggressief -25%/+35%, gebalanceerd -15%/+15%, voorzichtig -10%/+10%), `MAX_INTERNE_RONDES` = 5, `SCHADE_FRACTIE` = 0.35 (allemaal ongewijzigd, geen concreet probleem gevonden).
- DB-backed via `Instelling`-tabel: `ranked_gratis_per_dag` = 3 (ongewijzigd), `max_werkende_pets_per_speler` = **2** (was 3).

## Codebase-review (2026-07-28, verzoek van de gebruiker: "kijk ALLES na")

Volledige review van de productiecode (testscripts bewust overgeslagen op verzoek). Gevonden en **gefixt**:

- **Item-duplicatie bij ruilen (ernstigst)**: `_voer_kant_uit()` boekte items af met een ORM read-modify-write (`inv.aantal -= n`) terwijl de tegenhanger `_voeg_toe_aan_inventaris()` atomisch optelt (`SET aantal = aantal + n`). Bij twee gelijktijdige transacties (dubbelklik op "Definitief bevestigen" — de knoppen staan pas écht uit zodra `edit_message()` geland is) verloor de gever de items één keer en kreeg de ontvanger ze twee keer: **items uit het niets**. Opgelost met een nieuwe atomische helper `cogs/werk.py:_neem_uit_inventaris()` (`UPDATE ... WHERE aantal >= n` + rowcount-check) die nu overal gebruikt wordt waar voorraad afgaat: ruilen, `/craft`+`/shop` (`_koop_item`), `/verzorg`, `/uitrusten`, en het Extra match token. `_voer_kant_uit()` geeft nu een bool terug en de aanroeper doet een `rollback()` als één kant niet meer kan leveren, zodat er nooit een halve ruil blijft staan.
- **Tweede duplicatie-pad in de PvP-inzet**: `_verwerk_einde()` klemde de verliezer op `max(0, aantal - inzet)` maar schreef de winnaar altijd het volle bedrag bij — een gevecht duurt minuten, dus de verliezer kon zijn inzet intussen uitgeven. Nu wordt alleen bijgeschreven wat daadwerkelijk afgeboekt kon worden (zowel coins als items).
- **Clan werd onontbindbaar**: `/clan-verlaten` liet `oprichter_id` ongemoeid. Vertrok de oprichter terwijl er nog leden waren, dan kon niemand de clan nog ontbinden (de oprichter is geen lid meer, de leden zijn de oprichter niet) — en `/clan-info` toonde een "geest-oprichter". Het oprichterschap gaat nu automatisch over op het oudste resterende lid, met vermelding in het bericht en de log.
- **Dubbelklik-guards toegevoegd** aan `TradeVoorstelView` (accepteren/weigeren), `TradeBevestigView`, `CraftBevestigView` en `ReleaseBevestigView` — hetzelfde `_verwerkt`-patroon dat `cogs/gevechten.py` al had sinds de race-bug van 2026-07-26. Alleen die cog had ze; de rest niet.
- **Twee crash-paden**: `_verbruik_ranked_poging()` deed `inv.aantal -= 1` zonder None-check (crashte als de token tussen check en verbruik verdween — bij PvP zit daar de hele accepteer-stap tussen) en kon het aantal negatief maken; geeft nu een bool terug, en beide aanroepers breken het gevecht netjes af. `_kies_random_soort()` crashte met `IndexError` op een tier zonder pet-soorten; geeft nu een leesbare `ValueError` die `/spawn` al afvangt.

**Opruimwerk + performance (tweede ronde van dezelfde review):**

- **"Dode" velden bleken bewuste reserveringen** — belangrijke correctie op de eerste inschatting: `Speler.level`/`Speler.xp` en `Werkplek.vereiste_werk_genen` worden nergens gelezen, maar staan alle drie expliciet in de projectbrief (sectie 1: "Spelers — Currency, level, XP" en "Werkplekken — Type, vereiste werk_genen voor efficiëntie, ..."). Niet verwijderd dus; wél voorzien van een comment dat ze gereserveerd zijn en nog geen implementatie hebben, zodat een volgende lezer ze niet alsnog als dood hout opruimt. Idem voor `vang_cooldown_seconden`: de brief noemt een vang-cooldown, maar `cogs/vangen.py` leest die instelling nergens — **er is op dit moment dus geen vang-cooldown**. De beschrijving in de database is aangevuld met "(NOG NIET ACTIEF)". Alleen `MatchupResultaat.gevlucht` is echt weggehaald (puur intern dataclass-veld, nooit gezet én nooit gelezen); de HP-restanten blijven, met een comment dat ze informatief zijn.
- **Verouderde comments bijgewerkt**: de module-docstring van `utils/gevechten.py` beweerde nog dat de PvP-tegenstander passief "gebalanceerd" speelt (achterhaald sinds 2026-07-22, beide spelers kiezen zelf), `Werkplek.capaciteit` beschreef nog één globale pool (sinds het clan-systeem één per clan), en de `voerbak_niveau`-comment verwees naar "sync_stats() is een pure functie zonder DB-sessie" terwijl `sync_stats_met_voerbak()` juist wél een sessie gebruikt.
- **Afbeeldingen: gedeelde HTTP-sessie + LRU-cache** (`utils/afbeeldingen.py`): elke `download_afbeelding()` opende voorheen een eigen `aiohttp.ClientSession` en haalde het plaatje opnieuw op — een PvP-gevecht deed 6 downloads (3 matchups × 2 pets) van statische GitHub-URL's. Nu één gedeelde sessie plus een cache van 64 gedecodeerde afbeeldingen; gemeten scheelt dat ~449 ms → ~2 ms per herhaalde download. De cache geeft bewust een `.copy()` terug omdat aanroepers de afbeelding bewerken (bijsnijden/plakken). `bot.py` sluit de sessie netjes af in een `close()`-override, anders logt aiohttp bij shutdown een "Unclosed client session"-waarschuwing.
- **`scripts/seed.py` werkt instelling-beschrijvingen nu bij** bij een herrun (de *waarde* blijft bewust staan, want die kan via het admin panel afgestemd zijn) — voorheen bleef een gewijzigde beschrijving alleen in de code staan en niet in de database.

## Balans-hercontrole (2026-07-28, vraag van de gebruiker: "is alles nu netjes in balans?")

Het speeltempo doorgerekend met de productie-waarden (niet de dev-versnelling). Eén echte fout gevonden in de eerdere audit:

**De "~21 dagen tot level 50"-schatting klopte niet.** Die ging uit van *continu* overnacht-werken, maar dat kan een pet niet: een overnacht-shift kost 70 energie en energie herstelt alleen tijdens **rust** (+6/uur). Een pet werkt dus 10 uur en doet er daarna ~11,7 uur over om bij te komen — een cyclus van 21,7 uur, niet 10. Met `XP_PER_EFFECTIEVE_UUR = 95` kwam level 50 daardoor uit op **~45 dagen** (overnacht), 56 (lange shifts) of 72 (korte), ruim buiten het doel van 2-4 weken.

**Opgelost met optie 1 (keuze van de gebruiker): `XP_PER_EFFECTIEVE_UUR` 95 → 180.** Nieuw tempo naar level 50, inclusief energie-herstel:

| Shift | Zonder Zelfreinigend systeem | Met Zelfreinigend systeem |
|---|---|---|
| Korte | ~38 dagen | ~24 dagen |
| Lange | ~29 dagen | ~17 dagen |
| Overnacht | **~24 dagen** | ~13 dagen |

Coins- en grondstof-opbrengsten zijn hierdoor **niet** veranderd (XP staat los van de opbrengstberekening), dus de craft-economie blijft zoals na de audit.

**Twee observaties die bewust níét aangepast zijn** (geen bug, wel goed om te weten):

- **Korte shifts zijn ~4x efficiënter voor bonus-grondstoffen.** De 25%-kans geldt *per shift*, niet per uur: twee pets op korte shifts leveren ~2,2 bonus-grondstoffen per dag, op overnacht ~0,6. Daardoor bestaat er een verborgen optimale strategie (kort = craften, lang = XP). Dat is op zich een aardige keuze, maar hij staat nergens uitgelegd — kandidaat voor een regel in `/wiki` of een bewuste balansaanpassing (bijv. kans schalen met shiftduur).
- **Het Zelfreinigend systeem halveert de leveltijd** doordat energie ook tijdens werk herstelt. Dat maakt het veruit het krachtigste item in het spel, wat prima verdedigbaar is voor iets van 300 coins + 3 Sterrenstof + 20 Schroot — maar het is daarmee eerder een must-have dan een luxe.
- **Vechten levert nauwelijks XP** vergeleken met werken: één korte shift (720 XP) staat gelijk aan 24 gewonnen ranked-gevechten (`XP_WINST` = 30). Vechten heeft eigen beloningen (MMR, coins, inzet), dus dit is geen probleem zolang leveling bewust een werk-pad is.

## Admin panel: gemaakte keuzes (2026-07-29, historie)

De uitgebreide voorbereidingsnotitie die hier stond is vervangen door de uitkomst — het panel is gebouwd, zie "Wat werkt" en `docs/admin-portal-setup.md`. De vier vragen die vooraf met de gebruiker zijn doorgenomen, met hun antwoord:

1. **Scope**: gefaseerd. Fase 1 = alles wat al in de database stond (nul risico voor de spellogica), fase 2 = de ~50 hardcoded balansconstanten. Niet alles in één keer, want dat raakt bijna elk bestand in de bot voordat er iets werkend staat.
2. **Hosting**: de bot serveert het paneel zelf, met `critters.casualchaos.nl` als custom subdomein op Railway. Gekozen boven "HTML op casualchaos.nl + API op Railway" (het patroon van het bestaande paneel) omdat het handmatig kopiëren van HTML en de CORS-configuratie daarmee vervallen. Een pad als `casualchaos.nl/critters-admin` kan niet met DNS alleen.
3. **OAuth**: de bestaande Botv3-applicatie mag hergebruikt worden. Dat werkt (Discord staat meerdere redirect-URI's per app toe); nadelen zijn een gedeelde secret en dat het loginscherm de naam van die andere app toont. Alles zit in env-vars, dus later overstappen naar de eigen Chaos Critters-applicatie kost geen codewijziging.
4. **Spelerbeheer**: ja, volledig meegenomen — coins/MMR/items/pets/clans, niet alleen content en balans.

Aanvullende keuze tijdens het bouwen: **pet-soorten volledig beheersbaar maken** (toevoegen/bewerken/verwijderen), met een afbeelding-URL-veld i.p.v. upload. Dat vervangt de `scripts/seed.py` + `scripts/link_afbeeldingen.py`-route voor nieuwe soorten, zonder nieuwe infrastructuur (upload naar een Railway volume zou de plaatjes uit git halen en dus zonder versiebeheer/backup laten).

## Level-curve gefixt: vroege levels waren bijna gratis (2026-07-30)

Gemeld door de gebruiker met een concreet voorbeeld: een pet ging in één overnacht-shift (4680 XP) van level 1 naar **level 10**. Oorzaak: `xp_voor_volgend_level()` was lineair (`level × 100`), dus de eerste 9 levels kostten samen maar 4500 XP — bijna niets vergeleken met wat één shift oplevert (shifts leveren duizenden XP op sinds de `XP_PER_EFFECTIEVE_UUR`-verhoging naar 180).

**Fix**: curve veranderd naar `LEVEL_XP_BASIS (2000) + huidig_level × LEVEL_XP_PER_LEVEL (20)` (`utils/leveling.py`). Bewust zo gekozen dat het **totaal** aan XP voor level 1→50 exact gelijk blijft (122.500 XP, zelfde als de oude curve) — de eerder getunede "~24 dagen tot level 50"-balans blijft dus geldig, alleen nu gelijkmatig verdeeld (2020 XP voor level 1, 2980 voor level 49) in plaats van bijna gratis begin/heel duur einde (was 100 vs. 4900). Dezelfde gerapporteerde shift geeft nu level 1 → 3 in plaats van 1 → 10.

`scripts/test_leveling.py` gebruikte een hardcoded `1000` XP voor de "meerdere levels tegelijk"-test (paste bij de oude curve, niet bij de nieuwe) — vervangen door een dynamische berekening via `xp_voor_volgend_level()`, zodat de test bij toekomstige curve-aanpassingen blijft kloppen. `cogs/wiki.py`'s Leveling-onderwerp noemde de exacte oude formule (`huidig level × 100`) — dat is precies het soort concreet getal waarvan bij de wiki-review (2026-07-28) al was afgesproken het weg te laten omdat het bij balans-tweaks fout zou lopen; nu kwalitatief herschreven ("het kost-per-level loopt geleidelijk op"). Volledige testsuite (14 scripts) draait groen.

## Admin panel: alleen-lezen toegang voor spelers (2026-07-30, verzoek van de gebruiker: "openheid voor spelers")

Het panel was tot nu toe volledig admin-only, óók inloggen. Uitgebreid met een tweede toegangsniveau, in twee overlegrondes met de gebruiker afgestemd (toegang via Discord-login i.p.v. volledig publiek; dezelfde `admin.html` met een alleen-lezen modus i.p.v. een losse pagina).

- **`PortalSessie.is_admin`** (migratie `1b729b53b4a9`): elk lid van de `ADMIN_GUILD_ID`-server mag nu inloggen (`portal/auth.py:_lid_check`, was `_admin_check` en wees niet-admins af); of iemand admin is, wordt bij het inloggen bepaald via `member_is_admin()` en op de sessie vastgelegd — geen herberekening per request, dus een rolwijziging tijdens een lopende sessie vraagt om opnieuw inloggen.
- **Nieuwe `auth.vereist_admin()`-decorator**: wrapt route-handlers, geeft 403 als de sessie geen admin is. Toegepast op alle schrijf-routes (POST/DELETE) in `portal/api_content.py` en `portal/api_spelers.py`.
- **Wat leesbaar is zonder adminrol** (dezelfde categorie info die al publiek is via `/wiki`/`/critterdex`/`/clan-leaderboard`, nu met de actuele cijfers): Overzicht (incl. "Rijkste spelers" — bewust gehouden, expliciet verzoek van de gebruiker: volledige transparantie i.p.v. dat verbergen), Instellingen, Items & prijzen, Werkplekken, Tiers, Pet-soorten, Clans.
- **Wat admin-only blijft** (ook het ophalen, niet alleen schrijven): Spelers (individuele accounts: coins/MMR/inventaris/pets van specifieke spelers — privacygevoelig, geen spelbalans) en Kanalen (operationele configuratie, geen balans, toont bovendien kanaalnamen van alle servers waar de bot in zit).
- **Frontend (`web/admin.html`)**: puur CSS-gestuurd via een `body.readonly-mode`-class (i.p.v. per render-functie knoppen verbergen, want de content wordt dynamisch ververst) — verbergt `.save-row`, `.btn-danger` en `[data-write-action]` (de "+ Nieuwe soort"-knop, die niet in een save-row zit), zet alle formuliervelden op `pointer-events:none` (behalve zoekvelden, die blijven bruikbaar), en verbergt de Spelers/Kanalen-navigatie-items + het "Admin"-sectielabel via `[data-admin-only]`. Een `zetTab()`-guard voorkomt dat een niet-admin via een oude/geplakte URL-hash toch op die tabs belandt (de backend blokkeert het sowieso, dit voorkomt alleen een verwarrende lege/foutmelding-pagina). Klein "Alleen-lezen"-label naast de gebruikersnaam.
- **Getest** via een nieuwe `test_alleen_lezen_toegang()` in `scripts/test_portal.py`: een lid-sessie (`is_admin=False`) kan alle balans-/content-endpoints lezen maar krijgt 403 op Spelers/Kanalen (ook GET) en op elke geteste schrijf-actie (instellingen/items/soorten/clans), met een expliciete check dat een geweigerde schrijfpoging de database-waarde ook echt niet raakte. Volledige suite (14 scripts) draait groen.

## Wiki-tekst opgeschoond (2026-07-30, verzoek van de gebruiker)

Drie kleine correcties op `cogs/wiki.py`:

- **Werkplek-overzicht toegevoegd** aan "Werken & grondstoffen": een lijst welke hoofdgrondstof en bonus-grondstof elke werkplek oplevert (Moestuin→Groente/Fruit, Vijver→Algen/Water, Werkbank→Schroot/Spijker, Bos→Takken/Bladeren, Nachtwacht→Maanschijnkristal/Sterrenstof, Mijnschacht→Erts/Edelsteen), geverifieerd tegen de database in plaats van uit het geheugen overgetypt.
- **"werk_genen" hernoemd naar "werk-motivatie"** in speler-facing tekst (2x in "Vangen & Tiers", 1x in "Leveling") — de interne kolomnaam hoort niet in tekst die spelers lezen.
- **Alle em-dashes (—) verwijderd** uit de wiki-tekst (was 48x), vervangen door komma's, dubbele punten of haakjes al naar gelang de zin. Ook de command-lijst-separator ging van " — " naar ": ". Getest dat alle 9 onderwerpen nog binnen Discord's embed-limieten passen (grootste: 1320/4096 tekens voor "Werken & grondstoffen" na de toevoeging).

## Nieuw commando `/critter-stats` (2026-07-30, verzoek van de gebruiker, naar Botv3's `/mystats`)

Persoonlijk statistieken-overzicht, optioneel voor een ander lid (`speler`-parameter, zelfde patroon als Botv3). Toont: huidige pets + totaal ooit ontvangen (vangst + ruil, uit `Speler.volgend_pet_nummer`), Critterdex-percentage (unieke gevangen soorten / totaal soorten), aantal voltooide werk-shifts, PvP-winst/verlies + MMR, PvE-winst/verlies, en Chaos Coins.

**Twee nieuwe lifetime-tellers op `Speler`** (migratie `447da3feed31`), die nog nergens bestonden: `shiften_voltooid` (opgehoogd in `cogs/werk.py:_verwerk_lopende_shift` bij elke opgehaalde opbrengst) en `pvp_gewonnen`/`pvp_verloren`/`pve_gewonnen`/`pve_verloren` (opgehoogd in `cogs/gevechten.py:_verwerk_einde`, binnen hetzelfde `not is_friendly`-blok dat ook MMR/beloning/XP afschermt, dus vriendschappelijke gevechten tellen bewust niet mee — afgestemd met de gebruiker). **Beginnen voor iedereen op 0** vanaf deze toevoeging, geen terugwerkende kracht (expliciet akkoord van de gebruiker) — in tegenstelling tot pets/Critterdex-percentage, die wél met terugwerkende kracht correct zijn omdat ze uit al bestaande data worden afgeleid.

Genoemd naar "critter-stats" i.p.v. "mystats" op verzoek van de gebruiker, om verwarring met Botv3's eigen `/mystats` in dezelfde server te voorkomen.

Getest via nieuw `scripts/test_critterstats.py`: nette melding voor een speler die nog nooit gespeeld heeft, alle velden tonen de juiste cijfers (inclusief het Critterdex-percentage met dubbel-geteld-dezelfde-soort-toch-1x), de `speler`-parameter toont andermans stats, en een bot als doelwit wordt geweigerd. Volledige suite (15 scripts) draait groen.

## Nieuw commando `/changelog`, vervangt `/tests` (2026-07-30, verzoek van de gebruiker: "op de zelfde manier als Botv3")

Nieuwe cog `cogs/changelog.py`. Het oude `/tests` (`cogs/admin.py`) stuurde een `@everyone`-oproep direct de deur uit, zonder reviewstap — vervangen door een flow met een goedkeuringsstap, naar het patroon van Botv3's `/changelog`.

**Flow**: `/changelog [tag-rol]` (admin) opent een modal, voorgevuld met de nieuwste `## `-entry uit het nieuwe root-bestand `CHANGELOG.md` (zelfde parse-logica als Botv3: pakt alles tussen de eerste `## ` en de volgende, of het einde van het bestand). Bij versturen komt het voorstel in het reviewkanaal te staan met **Goedkeuren/Afwijzen/Bewerken**-knoppen. Goedkeuren splitst de tekst op een `### Voor admins & moderators`-kop (zelfde conventie als Botv3): het publieke gedeelte gaat naar het aankondigingskanaal (met een optionele rol-tag), het admin-gedeelte (indien aanwezig) naar een apart kanaal. Afwijzen markeert het voorstel als afgewezen zonder iets te posten. Bewerken opent een nieuwe modal met de huidige tekst en werkt het reviewbericht bij.

**Kanalen hardcoded, niet via `/setlog`** (2026-07-30, expliciet verzoek van de gebruiker: "niet met een command, programeer deze vast" — nadat eerst een `/setlog`-gebaseerde versie gebouwd was): `CHANGELOG_REVIEW_KANAAL_ID` = `1532720331750641684`, `CHANGELOG_AANKONDIGING_KANAAL_ID` = `1529099160526131215`, `CHANGELOG_ADMIN_KANAAL_ID` = `1532720331750641684` (zelfde ID als het reviewkanaal, expliciet zo opgegeven door de gebruiker; server-ID `1528670512664805529`) — alle drie als module-constanten in `cogs/changelog.py`, zelfde aanpak als Botv3 (die ook hardcodet, want draait ook maar op één hoofdserver). `/setlog` heeft daardoor geen changelog-categorieën meer nodig. De optionele rol-tag bij goedkeuring komt uit een nieuwe `Instelling`-rij `changelog_rol_id` (leeg = geen tag) — wél via de generieke instellingen-flow (portal/database), omdat een rol praktischer is om af en toe te wisselen dan een vast kanaal.

**Bewuste vereenvoudiging t.o.v. Botv3**: geen persistente (custom_id-based) view die een botherstart overleeft — dat past niet bij hoe de rest van deze bot interactieve UI behandelt (alles is expliciet niet-persistent, zie "Wat overleeft een redeploy" verderop). Een lopende review tijdens een redeploy moet dus opnieuw gestart worden met `/changelog`.

Getest via nieuw `scripts/test_changelog.py`: `_laatste_changelog_entry()`-parsing (met/zonder bestand, met meerdere entries), `split_changelog_content()` (met en zonder admin-kop), en de volledige modal→review→goedkeuren/afwijzen/bewerken-flow met een neppe Discord-client.

**`/todo` volledig verwijderd** (samen met `TODO_ITEMS` uit `cogs/algemeen.py`) — "wat is er nieuw"-communicatie loopt voortaan via `/changelog`. **`/craft-lijst` is niet langer tijdelijk**: blijft permanent staan (verzoek van de gebruiker), en is toegevoegd aan `/commands`. `/critter-stats` en `/changelog` zijn ook toegevoegd aan `/commands`.

## Admin panel fase 2, blok 1: balans-cache gebouwd en bewezen (2026-07-30)

Eerste stap van het fase 2-plan hierboven: nieuwe `utils/balans.py` met een in-memory cache van de `Instelling`-tabel. `laad()` vult de cache (aangeroepen bij bot-opstart in `bot.py:setup_hook`, vóór de cogs geladen worden), en `get_float(sleutel, default)`/`get_int(sleutel, default)` zijn synchrone getters die op de default terugvallen als de sleutel nog niet bestaat — dus een deploy vóórdat de seed opnieuw gedraaid is, verandert niets aan het gedrag. De portal roept `balans.laad()` opnieuw aan na elke `/api/instellingen`-wijziging (`portal/api_content.py:instellingen_opslaan`), rechtstreeks als functieaanroep omdat de portal in hetzelfde proces draait — geen polling nodig.

**Bewijs-blok**: `utils/elementen.py`'s `BONUS`/`MALUS` (was hardcoded 1.15/0.90) zijn nu `elementen_bonus`/`elementen_malus` in de `Instelling`-tabel, gelezen via `_bonus()`/`_malus()`-helperfuncties i.p.v. module-constanten. Handmatig geverifieerd dat de hele keten werkt: default vóór `laad()` → juiste waarde ná `laad()` → een directe databasewijziging heeft geen effect tot `balans.laad()` opnieuw draait → wél effect erna. Dit patroon (nieuwe `Instelling`-rij in `scripts/seed.py` + module-level constante vervangen door een functie die `balans.get_*` aanroept) is nu het sjabloon voor de resterende ~25 losse getallen uit de inventaris hierboven.

Volledige testsuite (16 scripts) draait groen, inclusief `test_elementen.py` (gedraaid zonder enige aanpassing nodig — bevestigt dat het gedrag exact hetzelfde bleef) en `test_portal.py` (de `/api/instellingen`-route inclusief de nieuwe herlaad-aanroep).

## Admin panel fase 2, blok 2: alle overige losse balansconstanten verhuisd (2026-07-30)

Vervolg op blok 1 (de balans-cache + het elementen-bewijs). Alle ~25 resterende losse getallen uit de fase 2-inventaris zijn nu `Instelling`-rijen, volgens hetzelfde sjabloon: module-constante → functie die `balans.get_int()`/`get_float()` aanroept met de oude hardcoded waarde als default.

**Per bestand:**
- `cogs/release.py`: `release_basis_coins`, `bonus_item_kans`.
- `utils/gevechten.py`: `max_interne_rondes`, `schade_fractie`, `elo_k`, `currency_basis_winst`, `currency_bonus_per_100_mmr`, `xp_winst`, `xp_verlies`, `energie_kost_min`, `energie_kost_max`, `ranked_reset_uur_echt`. `TACTIEK_VARIANTIE` blijft bewust hardcoded (dat is blok 5, gestructureerde data).
- `cogs/werk.py`: `currency_per_grondstof`, `bonus_grondstof_aantal`. `WERK_CYCLI` blijft bewust hardcoded (blok 3).
- `utils/leveling.py`: `xp_per_effectieve_uur`, `max_level`, `genen_groei_per_level`, `level_xp_basis`, `level_xp_per_level`.
- `utils/stats.py`: `honger_verval_minuten_echt`, `energie_herstel_minuten_echt`, `energie_minimum`, `slaap_cooldown_uur_echt`, `slaap_honger_kost`, `blessure_duur_uur_echt`. `HONGER_HERSTEL_WAARDEN`/`VOLLEDIG_HERSTEL_ITEMS`/`VOERBAK_ITEMS_PER_NIVEAU` blijven bewust hardcoded (blok 5).
- **Gedeelde sleutel** `dev_versnelling` (was 2x los `_DEV_VERSNELLING = 120` in `utils/stats.py` én `utils/gevechten.py`): nu écht één instelling voor beide, in plaats van twee constanten die toevallig dezelfde waarde hadden en uit de pas hadden kunnen lopen.

**De aangekondigde valkuil trad precies op zoals voorspeld**: `RANKED_RESET_UUR` (`utils/gevechten.py`), `HONGER_VERVAL_MINUTEN`/`ENERGIE_HERSTEL_MINUTEN`/`SLAAP_COOLDOWN_UUR`/`BLESSURE_DUUR_UUR` (`utils/stats.py`) waren afgeleide module-constanten (dev-versnelling er al op import-tijd in verwerkt) — die moesten functies worden (`ranked_reset_uur()`, `honger_verval_minuten()`, enz.), anders zou de waarde bevriezen op het moment vóór de eerste `balans.laad()`-aanroep.

**Alle call-sites bijgewerkt** in `cogs/gevechten.py`, `cogs/verzorging.py`, `scripts/test_item_overhaul.py`, `scripts/test_verzorging.py`, `scripts/test_leveling.py` — importeren nu de functie i.p.v. de oude constante, en roepen 'm aan waar nodig.

**Getest**: volledige suite (16 scripts) draait groen zonder dat de tests zelf `balans.laad()` hoeven aan te roepen (de cache valt terug op de hardcoded default zolang er niet expliciet geladen is, en die defaults zijn identiek aan de oude constanten — dus ongewijzigd testgedrag). Daarnaast een handmatige end-to-end-check (zelfde soort proef als bij blok 1) voor een steekproef van de nieuwe sleutels (`xp_per_effectieve_uur`, `max_level`, `xp_winst`, `energie_kost_min`, `ranked_reset_uur`, `honger_verval_minuten`, `slaap_cooldown_uur`): defaults kloppen, de dev-versnelling wordt correct toegepast, en een live databasewijziging heeft pas effect na `balans.laad()`.

**Nog open van de fase 2-inventaris**: blok 3 (`WERK_CYCLI`, eigen tabel), blok 4 (`RECEPT_KOSTEN`, eigen tabel met FK's), blok 5 (`TACTIEK_VARIANTIE` + voer-effecten). Zie het aanpak-advies verderop in dit document voor de bekende valkuilen daarbij.

## Admin panel fase 2, blok 3: WERK_CYCLI naar een eigen tabel (2026-07-30)

Het lastigste geval uit het fase 2-plan, precies om de voorspelde redenen. `WERK_CYCLI` was een dict van `frozen=True` dataclasses die op **import-tijd** werd opgebouwd, met de dev-versnelling er al in verwerkt, en werd vanuit meerdere modules geïmporteerd.

**Nieuwe `werk_cycli`-tabel** (migratie `b16522d16fa3`) met per rij `sleutel`, `label`, `duur_uren`, `energie_kost`, `output_multiplier`, `volgorde`. Bewust een eigen tabel i.p.v. platte `Instelling`-sleutels: dit is gestructureerde data (4 waarden per cyclus), en zo kan de portal er een rij-editor voor geven. De migratie **voegt de 3 bestaande cycli meteen in** met exact de oude waarden — de bot draait bij opstart wel migraties maar niet de seed, dus anders zou de tabel op productie leeg blijven.

**`Cyclus` verhuisd van `cogs/werk.py` naar `utils/balans.py`**: `werk_cycli()` moet 'm kunnen opbouwen, en andersom zou een circulaire import ontstaan (`cogs/werk.py` importeert al `utils.balans`). `balans.laad()` cachet nu zowel de `Instelling`-rijen als de `WerkCyclus`-rijen. `duur_uren` in de database is altijd de **echte** duur; de dev-versnelling (1 minuut per shift) wordt pas in `werk_cycli()` toegepast, zodat `reward_duur_uren` — en dus de opbrengstberekening — altijd met de echte waarde rekent. Bij een lege tabel valt `werk_cycli()` terug op `_STANDAARD_CYCLI` met exact de oude hardcoded waarden.

**`/werk`'s cyclus-parameter is van `app_commands.Choice` naar autocomplete gegaan.** De oude keuzelijst had de duur hardcoded in de labels ("Korte shift (2 uur)"), en `Choice`-namen worden op command-sync-tijd vastgelegd — die zouden dus stilletjes verouderen zodra iemand de duur via de portal bijstelt. De autocomplete leest elke keer de actuele waarden en toont nu ook de energiekosten. Gevolg: `cyclus` is een gewone `str` geworden, dus er is **server-side validatie** bijgekomen (autocomplete dwingt geen geldige waarde af — een speler kan vrije tekst versturen zonder een suggestie te kiezen). `werkplek` blijft wél een `Choice`-lijst: daar is alleen de naam relevant en die is niet aanpasbaar.

**Defensief bij een onbekende cyclus**: `balans.werk_cycli()[...]` is op de drie plekken die een opgeslagen `Huisdier.werk_cyclus` opzoeken vervangen door `.get()` met een nette afhandeling. Dat kan alleen als een cyclus handmatig uit de database verdwijnt (de portal kan niet toevoegen/verwijderen), maar in `_stuur_klaar_notificaties` zou een `KeyError` de achtergrondtaak permanent stilleggen voor álle spelers — dat risico is het niet waard.

**Portal**: nieuwe tab "Werk-shifts" met `GET /api/werk-cycli` (leesbaar voor elk ingelogd lid) en `POST /api/werk-cycli/{sleutel}` (admin-only). De sleutel is read-only in het paneel, met een uitleg waarom. Het paneel toont per shift ook de afgeleide **effectieve uren** (duur × multiplier), want dat getal bepaalt rechtstreeks de opbrengst.

**Getest** via een nieuwe `test_werk_cycli()` in `scripts/test_portal.py`: lezen, validatie (duur 0/500u, energie 500, multiplier 0, leeg label, onbekende sleutel), en een echte wijziging waarbij geverifieerd wordt dat `balans.werk_cycli()` **meteen** de nieuwe waarde teruggeeft zonder herstart. De rechten-check-test dekt de nieuwe endpoints ook (lezen mag een lid, schrijven geeft 403). `scripts/test_clan.py` en `scripts/test_werk.py` zijn aangepast omdat `cyclus` geen `Choice` meer is. Volledige suite (16 scripts) groen.

**Nog open van fase 2**: blok 4 (`RECEPT_KOSTEN`, eigen tabel met FK's naar items) en blok 5 (`TACTIEK_VARIANTIE` + de voer-effecten).

## Pet-soort Faisant hernoemd naar Fazant (2026-07-30, verzoek van de gebruiker)

Spelfout. Gedaan als **migratie** (`387e1c3602fe`) i.p.v. handmatig in de database zoals bij eerdere hernoemingen (Lemuur → Maki), zodat het ook vanzelf op productie landt bij de eerstvolgende deploy — de bot draait bij opstart wel migraties maar niet `scripts/seed.py`.

Moest een `UPDATE` zijn en geen "verwijderen + opnieuw seeden": `seed.py` gebruikt `INSERT ... ON CONFLICT DO NOTHING` op `naam`, dus alleen de seed bijwerken zou de oude rij laten staan én een tweede rij "Fazant" toevoegen. De afbeelding is meegehernoemd (`docs/assets/faisant.png` → `fazant.png`) en de migratie werkt `afbeelding_url` mee bij. Bewust in die volgorde gecommit: het plaatje stond op GitHub vóórdat de nieuwe URL ergens gekoppeld werd, want Discord's media-proxy cachet ook mislukte fetches per exacte URL. Beide branches (`dev` en `main`) geverifieerd met een HTTP-check: nieuwe URL 200, oude 404. Nul gevangen exemplaren op het moment van hernoemen.

## Admin panel fase 2, blok 4: RECEPT_KOSTEN naar een eigen tabel (2026-07-30)

Nieuwe `recepten`-tabel (migratie `584df3c7d2a4`): één rij per ingrediënt, met **echte FK's** naar `items` voor zowel het te maken item als de grondstof (`ondelete=CASCADE` resp. `RESTRICT`), plus een unieke constraint op (item, grondstof). Dat was het advies uit het fase 2-plan en het is hier duidelijk beter dan platte sleutels: dit ís een relatie tussen items, de database bewaakt nu dat beide kanten bestaan, en de portal kan dropdowns tonen in plaats van vrije tekstvelden waar je een naam in kunt typen die nergens op slaat. De migratie zet de 10 bestaande recepten (20 ingrediënt-rijen) meteen over door op naam op te zoeken; ontbrekende namen worden overgeslagen i.p.v. de migratie te laten crashen (een ontbrekend recept is herstelbaar via de portal, een gecrashte migratie betekent dat de bot niet opstart).

`balans.laad()` cachet de recepten platgeslagen als `itemnaam -> [(grondstofnaam, aantal)]`, precies de vorm die de oude `RECEPT_KOSTEN`-dict had — zo hoefden de call-sites in `cogs/verzorging.py` alleen van `RECEPT_KOSTEN` naar `balans.recepten()` te veranderen, zonder verdere herschrijving.

**Bewust géén hardcoded fallback** (anders dan bij de werk-cycli): een lege recepten-tabel betekent hier "geen enkel item kost grondstoffen", wat zichtbaar is en herstelbaar via de portal. Stilletjes terugvallen op oude waarden zou juist verwarrend zijn als iemand bewust een recept weghaalt. **Gevolg voor de tests**: vier testscripts moesten expliciet `balans.laad()` gaan aanroepen. `test_item_overhaul.py` viel hier ook echt over — die controleert dat een Slimme voerbak Schroot vereist, en zonder geladen cache verdween die eis stilletjes. Precies de faalmodus die deze keuze zichtbaar maakt.

**Portal**: nieuwe tab "Recepten" met `GET /api/recepten` (leesbaar voor elk ingelogd lid) en `POST /api/recepten/{item_id}` (admin-only). De POST vervangt het volledige recept in één keer (delete + insert) i.p.v. per ingrediënt te patchen — een recept is een geheel, en zo zijn er geen losse toevoeg-/verwijder-endpoints nodig. Ook items zónder recept worden getoond, zodat je er een kunt aanmaken. Validatie weigert: aantal buiten 1..10.000, onbekende grondstof, een item dat zichzelf als ingrediënt heeft, en dezelfde grondstof twee keer in één recept.

Getest via een nieuwe `test_recepten()` in `scripts/test_portal.py`: lezen, alle vijf validatiegevallen, een echte wijziging met verificatie dat `balans.recepten()` die meteen ziet, en een leeg recept opslaan (item kost dan alleen Chaos Coins). Volledige suite (16 scripts) groen.

**Nog open van fase 2**: alleen blok 5 (`TACTIEK_VARIANTIE` + de voer-effecten `HONGER_HERSTEL_WAARDEN`/`VOLLEDIG_HERSTEL_ITEMS`/`VOERBAK_ITEMS_PER_NIVEAU`). **Afgerond op 2026-07-30, zie verderop.**

## Actieve spawns overleven een herstart (2026-07-30, verzoek van de gebruiker)

Was een openstaand backlog-punt: `VangenCog.actieve_spawns` stond alleen in het geheugen, dus elke redeploy maakte de lopende spawn onvangbaar — de embed bleef in Discord staan, maar `/vang` antwoordde "geen spawn actief". Bij een actieve testgroep sneuvelde er dus potentieel een pet bij elke deploy.

Nieuwe `actieve_spawns`-tabel (migratie `3a145a5dbc05`) met `channel_id` als primary key (er is per kanaal hooguit één actieve spawn — een nieuwe vervangt de oude, wat nu een `ON CONFLICT DO UPDATE` is). De tabel spiegelt de in-memory dict: schrijven bij een nieuwe spawn, wissen bij een vangst én bij `/verwijderspawnkanaal`, en inladen in `cog_load()`.

**De interessantste wijziging zat in het bijwerken van de embed.** `actieve_spawns` bewaarde een `discord.Message`-object, maar na een herstart is er alleen nog een message-ID in de database. Daarom bewaart de dict nu `(soort, message_id)` en gebruiken `_markeer_gevangen`/`_markeer_verlopen` een **`PartialMessage`** (`channel.get_partial_message(id)`), die kan bewerken zonder het bericht eerst op te halen — geen extra API-calls bij het opstarten. Gevolg: die functies konden niet langer de bestaande embed aanpassen (een `PartialMessage` heeft geen `.embeds`), dus ze **bouwen de embed nu opnieuw op**. Dat kan probleemloos, want alles wat erin staat — naam, tier-kleur, afbeelding — hangt al aan de soort; `soort.tier_id` levert de kleur, dus daar is zelfs geen extra query voor nodig.

Getest via nieuw `scripts/test_spawn_persistentie.py`, dat een herstart nabootst door een **tweede `VangenCog`-instantie** te maken en `cog_load()` te draaien (precies wat een deploy doet: nieuw proces, lege dicts, alleen de database blijft). Drie scenario's: de spawn is na de "herstart" nog steeds bekend; `/vang` werkt daarna en ruimt zowel de dict als de databaserij op (met verificatie dat de embed via het `PartialMessage`-pad is bijgewerkt); en een nieuwe spawn vervangt de oude netjes in één rij. Volledige suite (17 scripts) groen.

## Admin panel fase 2, blok 5: voer-effecten en tactiek-variantie (2026-07-30) — fase 2 afgerond

Het laatste blok, en meteen het blok waar de twee helften een tegengestelde keuze kregen.

**Voer-effecten → kolommen op `items`** (migratie `8dd39652036a`). `HONGER_HERSTEL_WAARDEN`, `VOLLEDIG_HERSTEL_ITEMS` en `VOERBAK_ITEMS_PER_NIVEAU` (`utils/stats.py`) waren alle drie op *itemnaam* gesleuteld, dus die data hoorde gewoon bij het item zelf: `items.honger_herstel` en `items.voerbak_vanaf`. Twee dingen verdwijnen daarmee:

- `VOLLEDIG_HERSTEL_ITEMS` gaat op in `honger_herstel = 100`. Honger wordt toch al op 100 geklemd, dus het resultaat is identiek — een aparte "volledig"-vlag was een tweede manier om hetzelfde te zeggen.
- De **volgorde** waarin een voerbak voer opeet hoefde niet opgeslagen te worden: dat is "goedkoopste eerst", en de bestaande prijzen (10 / 35 / 60) reproduceren de oude hardcoded volgorde exact. `balans.voerbak_voer()` sorteert dus op `prijs`.

`voerbak_vanaf` is `NULL` (nooit automatisch), `"simpel"` (beide voerbakken) of `"slim"` (alleen de Slimme). De Mysterie voedselzak blijft bewust buiten deze kolommen: die heeft geen eigen honger-effect maar simuleert bij gebruik een willekeurig ánder voedingsitem, en blijft daarmee een hardcoded speciaal geval in `cogs/verzorging.py`.

**`TACTIEK_VARIANTIE` → zes losse `Instelling`-sleutels** (migratie `5f0c1b7a94e2`), en dus géén eigen tabel — anders dan het oorspronkelijke advies hierboven. Reden: de drie tactieken staan hardcoded in de keuzemenu's van `/pvp` en `/pve`, dus een rij toevoegen of weghalen zou niets doen. Alleen de zes getallen zijn zinvol aanpasbaar. `balans.tactiek_variantie(tactiek)` valt bij een onbekende tactiek terug op "gebalanceerd", net als voorheen.

**Nog een `app_commands.Choice` die stale zou worden**: `/verzorg item` was een vaste Choice-lijst opgebouwd uit de oude dicts. Nu welke items voeding zijn uit de database komt, zou die lijst op command-sync-tijd bevriezen — hetzelfde probleem als bij `/werk cyclus` in blok 3, en met dezelfde oplossing: autocomplete (die toont er meteen `+15 honger` bij) plus server-side validatie, want autocomplete accepteert vrije tekst.

**Validatie in de portal**: leeg `honger_herstel` betekent "dit item is geen voer", en dan wordt `voerbak_vanaf` óók leeggemaakt. Anders zou een voerbak een item pakken dat niets doet — of erger, een `KeyError` geven in `sync_stats_met_voerbak`. De items-tab toont de twee velden alleen bij items van het type voeding. `item_opslaan` roept nu ook `balans.laad()` aan; dat was niet nodig zolang alleen prijs en beschrijving aanpasbaar waren.

Getest via nieuw `scripts/test_voer_en_tactiek.py` (waarden identiek aan vóór de verhuizing, een echte wijziging werkt door in voerbak én `/verzorg`, geen item met een voerbak-niveau maar zonder effect, en `macht_met_tactiek` blijft over 500 trekkingen binnen de ingestelde grenzen) plus een `test_voer_effecten()` in `scripts/test_portal.py`. Volledige suite (18 scripts) groen.

**Fase 2 is hiermee afgerond**: alle balansconstanten uit de inventaris bovenaan dit document staan nu in de database en zijn via het portal aanpasbaar zonder deploy.

## 25 nieuwe pet-soorten, negende lichting (2026-08-04, verzoek van de gebruiker: "verzin deze zelf")

**Nu 175 pet-soorten** (was 150): 56 Common (+8), 36 Uncommon (+5), 42 Rare (+6), 25 Epic (+4), 16 Legendary (+2) — verdeling geschaald naar de bestaande tier-verhouding (48:31:36:21:14 → afgerond 8:5:6:4:2 op 25 nieuwe).

Volledig zelf verzonnen (naam + stats + werkplek + beschrijving + element), met als expliciete leidraad [[feedback_pet_design_variety]]: geen extra vossen/honden/katachtigen/primaten/beren/runderachtigen (die families zaten al ruim vertegenwoordigd), wel bewust frisse hoeken:

- **Spinachtigen**: Wolfspin (Uncommon) naast de al bestaande Schorpioen (die er in dezelfde lichting bij kwam) — twee totaal verschillende jachtstijlen (web-loze actieve jager vs. gifangel), geen overlap met bestaande soorten.
- **Cephalopoden**: Zeekat/inktvis (Uncommon) naast de bestaande Chaos Octopus — zeekat is qua uiterlijk (torpedo-lijf, kleurverandering) duidelijk een ander dier dan een octopus.
- **Bewust beperkte overlap waar het toch gebeurde**: Orka (Epic, cetacea) is de derde walvisachtige naast Dolfijn/Walvis, maar orka's zijn zo herkenbaar anders (zwart/wit, apex-jager) dat het geen "stapeling" voelt. Reuzenmiereneter (Epic) en Mierenegel zijn allebei mierenetend als de bestaande Aardvarken, maar drie totaal verschillende dierfamilies (Vermilingua/Tachyglossidae/Tubulidentata) met een compleet ander uiterlijk.

**Correctie, zelfde dag**: eerste versie bevatte Feniks (Epic) en Griffioen (Legendary), twee niet-Chaos mythische wezens. Gebruiker: "Griffioen gaat me echt te ver, eenhoorn is grappig want meme" — de bestaande Chaos Eenhoorn overleeft dus specifiek omdat hij een gevestigde grap/meme in de bot is, niet omdat mythische wezens in het algemeen zouden mogen. **Dit is exact dezelfde fout als op 2026-07-24** (toen ook al Griffioen + Chaos Basilisk afgekeurd om deze reden, zie [[feedback_pet_design_variety]] die dat al vastlegde) — het advies bestond dus al, maar is niet toegepast bij het opstellen van deze lichting. Memory-bestand aangescherpt naar een harde regel i.p.v. een afweging. Beide vervangen door echte dieren met een chaos-hoek: **Chaos Wandelende Tak** (Epic, stokinsect — camouflage past mooi bij het "wisselt dagelijks"-thema) en **Chaos Casuaris** (Legendary, cassowary — al buiten de bot bekend als "gevaarlijkste vogel ter wereld", geeft dezelfde wildcard-lol zonder fantasie-wezen te zijn). Tegelijk ook de vraag beantwoord "geen chaos pets?" — deze lichting had er anders nul gehad.

**Volledige lijst per tier:**

| Common (8) | Uncommon (5) | Rare (6) | Epic (4) | Legendary (2) |
|---|---|---|---|---|
| Zwaluw | Wolfspin | Komodovaraan | Orka | Mammoet |
| IJsvogel | Zeekat | Vleermuisvos | Chaos Wandelende Tak | Chaos Casuaris |
| Vuurvlieg | Tapir | Buideldas | Reuzenschildpad | |
| Schorpioen | Zee-egel | Neushoornkever | Reuzenmiereneter | |
| Watervlo | Muntjak | Walvishaai | | |
| Zeepaling | | Zeeslak | | |
| Grutto | | | | |
| Kwikstaart | | | | |

Elementen volgens het bestaande patroon: vliegende dieren (Zwaluw, IJsvogel, Vuurvlieg, Grutto, Kwikstaart, Vleermuisvos) → lucht; waterdieren (Watervlo, Zeepaling, Zeekat, Zee-egel, Walvishaai, Zeeslak) → water; landdieren (Wolfspin, Tapir, Muntjak, Komodovaraan, Neushoornkever, Reuzenschildpad, Reuzenmiereneter, Mammoet) → grond; "klein/aquatisch maar gevaarlijk" of "agressieve jager" (Schorpioen, Buideldas, Orka) → vuur, zelfde uitzonderingspatroon als de bestaande Vuurvis/Mantisgarnaal; de twee Chaos-soorten → chaos.

Toegevoegd aan `scripts/seed.py` (`PET_SOORTEN` + `ELEMENT_MAP`) en tegen de dev-database geseed (Feniks/Griffioen waren nog door niemand gevangen, dus rechtstreeks verwijderd i.p.v. omgezet). **Afbeeldingen toegevoegd en gekoppeld (2026-08-05)** — twee bestanden hernoemd naar de juiste spelling vóór het koppelen (`gruto.png` → `grutto.png`, `kwikstraat.png` → `kwikstaart.png`), 25 nieuwe slugs toegevoegd aan `scripts/link_afbeeldingen.py`, afbeeldingen eerst gepusht naar GitHub (zelfde reden als de cache-waarschuwing hieronder), dan gekoppeld. Alle **175 soorten** hebben nu een eigen afbeelding, geen placeholders meer.

`scripts/test_critterdex.py` had de paginering-check hardcoded op "150 soorten" staan; dat brak meteen zichtbaar bij deze uitbreiding (precies het soort regressie die je wil zien falen). Gefixt door het aantal soorten in de test zelf op te vragen in plaats van hard te coderen — telt voortaan automatisch mee bij toekomstige lichtingen. Volledige suite gecheckt: alleen de twee al langer bekende, hier ongerelateerde issues (`test_vangen.py` kapot door een verwijderde functie, `test_pvp_namen.py`'s harmless "unclosed session"-warning na een geslaagde run) waren nog aanwezig.

## Dagelijkse opdrachten (2026-08-05, verzoek van de gebruiker)

Aanleiding: het spel had veel systemen maar geen dagelijkse *richting* — een speler logt in en moet zelf bedenken wat te doen. Ook stond "dagelijkse activiteit/login bonus" al sinds het begin in de brief (sectie 13) als currency-bron zonder ooit gebouwd te zijn.

Elke speler krijgt **drie willekeurige opdrachten** uit een pool van zes, die op een vast tijdstip voor iedereen tegelijk resetten. Elke afgeronde opdracht geeft Chaos Coins, alle drie geeft een bonus. Nieuw commando `/opdrachten` (ephemeral, met voortgangsbalk), nieuwe cog `cogs/opdrachten.py`, logica in `utils/opdrachten.py`.

**De belangrijkste ontwerpkeuze: voortgang ophogen, niet afleiden.** De verleiding was om de bestaande tellers te gebruiken (`Speler.shiften_voltooid`, `pvp_gewonnen`, het aantal `Huisdier`-rijen). Dat gaat mis voor "vang 3 critters": `/critter-stats` telt je *huidige* pets, dus een `/release` zou de voortgang weer omlaag brengen. Nu houdt elke opdracht z'n eigen voortgang bij, opgehoogd op het moment van de gebeurtenis — niet te omzeilen en onafhankelijk van wat je daarna met de pet doet.

**Vaste dag, maar niet om middernacht.** De dag draait om `opdracht_reset_uur` (standaard 04:00 Amsterdam): wie 's avonds laat speelt raakt zo niet midden in een sessie z'n voortgang kwijt. Bewust een vast moment voor iedereen i.p.v. het rollende 24-uursvenster dat de ranked-pogingen gebruiken — bij opdrachten wil je dat spelers dezelfde "dag" delen.

**Toewijzing is lui**: de drie opdrachten worden aangemaakt bij de eerste actie of `/opdrachten` van die dag. Geen achtergrondtaak die voor álle spelers rijen zit te maken, ook voor wie die dag niet speelt.

**De "alle drie af"-bonus heeft geen eigen kolom of tabel.** Die wordt uitbetaald op het moment dat de laatste opdracht van `voltooid_op = NULL` naar een tijdstip gaat. Die overgang gebeurt per definitie precies één keer, dus dubbel uitbetalen kan niet — een `bonus_uitbetaald`-vlag zou alleen maar een tweede bron van waarheid zijn.

**`doel` en `beloning` zijn een momentopname** bij het toewijzen, geen live lookup: een balanswijziging via het portal verplaatst zo niet halverwege de dag de doelpaal van een opdracht die al loopt.

Increment-hooks zitten in `/vang` (+ `zeldzaam_vangen` bij tier ≥ 3), het afronden van een shift in `cogs/werk.py`, het einde van een gevecht in `cogs/gevechten.py` (alleen de winnaar, en al achter de bestaande `not is_friendly`), `/verzorg` met voeding, en de bevestigknop van `/craft`. Alle vijf in dezelfde transactie als de actie zelf, behalve craft: `_koop_item` commit daar al zelf, dus dat is een losse tweede commit — de aankoop mag niet meer afhangen van de opdracht-bijwerking. Craft telt bewust alleen via `/craft` en niet via `/shop`, ook al gebruiken beide `_koop_item`.

**Opdracht-types staan hardcoded**, alleen doelen/beloningen zijn instelbaar (14 nieuwe `Instelling`-sleutels, migratie `ff40d530dd7f`). Zelfde afweging als bij de tactiek-variantie in fase 2 blok 5: elk type heeft een `verhoog()`-aanroep op de juiste plek in de code nodig, dus een rij toevoegen via het portal zou niets doen. De sleutels verschijnen vanzelf in de instellingen-tab, die alle `Instelling`-rijen toont.

Elk type heeft een enkelvouds- én meervoudsvorm, want de doelen zijn aanpasbaar en Nederlandse meervouden zijn niet uniform (gevecht → gevechten, shift → shifts, keer → keer). Eén template met een losse "s" gaf "Win 1 gevechten".

Getest via nieuw `scripts/test_opdrachten.py`: drie per dag en stabiel bij herhaald ophalen (anders kon je door `/opdrachten` te spammen herrollen tot je een makkelijke set had), voortgang/uitbetaling precies één keer, de bonus exact bij de laatste, een nieuwe dag met de oude nog intact, de dag-grens op het resetuur i.p.v. middernacht, een onbekende sleutel als harde fout, en de cog zelf via een nagebootste interactie. Volledige suite (19 scripts) groen.

**Volgende stap, al afgesproken**: chaos events — tijdelijke server-brede modifiers, te starten vanuit het portal. Inclusief een **incense-event** naar Pokémon-voorbeeld waarbij de spawn-timer flink omlaag gaat; dat past goed omdat spawns op berichten-tellers werken (`spawn_interval_min_berichten` / `spawn_interval_max_berichten`) die sinds fase 2 al als instelling in de database staan — een incense is dus letterlijk die twee tijdelijk verlagen. Daarna eventueel een **zeldzaam item** waarmee een speler zelf een incense kan activeren; dat item moet expliciet heel moeilijk te krijgen zijn.

## Chaos events (2026-08-05, verzoek van de gebruiker)

Tijdelijke, server-brede modifiers. Het incense-idee kwam van de gebruiker ("een soort hoe het bij Pokémon heet Insence event, waarbij de spawn timer drastisch wordt verlaagd"), de rest is in overleg gekozen. Vier types in de eerste lichting:

| Event | Effect | Standaardsterkte |
|---|---|---|
| 🌫️ Incense | Spawn-drempel × factor, dus sneller spawnen | 0.25 (4x sneller) |
| 🌠 Sterrenregen | Spawnkans van Rare en hoger × factor | 3.0 |
| 🌾 Grondstoffenregen | Grondstoffen per shift × factor | 2.0 |
| 💰 Muntregen | Chaos Coins uit werk én gevechten × factor | 2.0 |

**Alleen handmatig te starten vanuit het portal** (keuze van de gebruiker). Bewust geen willekeurige automatische events: een incense die loopt terwijl er niemand online is, is puur verspild.

**Effecten lopen vanzelf af.** "Is dit event actief" is puur `eindigt_op > nu`, dus er is geen achtergrondtaak nodig om iets uit te zetten en een herstart midden in een event verandert niets. De achtergrondtaak die er wél is (`cogs/events.py`) doet alleen de "voorbij"-aankondiging — als die zou sneuvelen blijft het spel gewoon kloppen.

**`factor()` geeft 1.0 als er niets loopt**, zodat elke aanroeper onvoorwaardelijk kan vermenigvuldigen zonder een `if`. Cache-patroon gelijk aan `utils/balans.py`: alles in het geheugen, herladen bij opstarten en zodra de portal iets start of stopt.

**De sterkte is een momentopname** bij de start, net als bij de dagopdrachten: een balanswijziging halverwege verandert niet de spelregels van een lopend event.

**Grondstoffenregen mag de coins niet meeverdubbelen.** In `cogs/werk.py` werden de coins afgeleid uit `grondstof_aantal`, dus een grondstoffenregen zou er stilletjes ook een muntregen bij zijn geweest. Nu worden de coins uit de **basis**-opbrengst berekend en wordt de grondstof-factor daarna apart toegepast. Er staat een expliciete test op.

**Meegenomen opruiming**: `_nieuwe_drempel()` in `cogs/vangen.py` las de spawn-interval-instellingen nog met een eigen DB-query rechtstreeks uit de `Instelling`-tabel — een overblijfsel van vóór fase 2. Nu via de balans-cache, en daardoor ook synchroon (de twee aanroepers hoefden er niets meer voor te awaiten).

**Aankondigingen** gaan naar alle spawn-kanalen plus een kanaal dat je per event kiest in het portal (verzoek van de gebruiker: "spawn kanaal en kanaal naar keuze, dus tijdelijk kanaal"). Fouten per kanaal worden gelogd maar stoppen de rest niet — één kanaal zonder schrijfrechten mag de aankondiging niet voor iedereen blokkeren. De einde-melding zet z'n `einde_gemeld`-vlag en commit vóór het versturen, anders zou een trage Discord-call hetzelfde event tweemaal laten afmelden.

**Portal**: nieuwe tab "Chaos events" met de lopende events, een startformulier per type (duur + optioneel aankondigingskanaal) en de laatste 25 uit de geschiedenis. Lezen mag elk ingelogd lid (events zijn toch publiek aangekondigd), starten en stoppen is admin-only. Een tweede event van hetzelfde type tegelijk wordt geweigerd — `actief()` zou er toch maar één pakken en twee incenses zijn vooral verwarrend in de aankondigingen. Verschillende types naast elkaar mag wél. Stoppen zet `eindigt_op` op nu i.p.v. de rij te verwijderen, zodat de geschiedenis blijft staan en de einde-aankondiging nog gedaan wordt.

Getest via nieuw `scripts/test_events.py` (neutraal zonder event, incense verlaagt de drempel echt van ~32 naar ~8 berichten, sterrenregen tilt het aandeel Rare+ van 24% naar 58%, grondstoffen en coins raken elkaar niet, een verlopen event is vanzelf niet meer actief zónder herladen, en de sterkte-momentopname) plus `test_events()` in `scripts/test_portal.py` voor de endpoints. Volledige suite (21 scripts) groen.

`scripts/test_spawn_persistentie.py` viel om op de nieuwe `followup.send` in `/vang` (de nep-interactie mockte `followup` niet) — fixture aangevuld. Precies waar zo'n test voor is.

### Herzien nog dezelfde dag: per kanaal + instelbare sterkte

Drie stukken feedback direct na de eerste versie, allemaal verwerkt:

**1. "0.25 klinkt niet als 4x".** Het portal toonde de rauwe factor. Die is voor een incense 0.25 (de drempel gaat maal 0.25) terwijl je "4x sneller" bedoelt. Overal waar een mens het getal ziet of intypt wordt nu de **zichtbare** vorm gebruikt (`naar_zichtbaar()` / `naar_factor()` op `EventType`); alleen de database en de berekeningen werken met de rauwe factor.

**2. Aankondiging maakte niet duidelijk waar het gold.** De tekst zei "in de spawn-kanalen" zonder te noemen welke, dus in een willekeurig kanaal las het alsof het event daar gold. Spawn-gebonden events noemen nu expliciet de kanalen met aanklikbare mentions; niet-spawn-gebonden events zeggen "geldt overal, voor iedereen".

**3. Spawn-events moesten per kanaal** ("ik wil per kanaal kunnen kiezen, om bijvoorbeeld een event kanaal te kunnen gebruiken LOS van de spawn kanalen"). Dat is de grootste wijziging: `Event.kanaal_id` (migratie `30e55efa5f53`, NULL = overal), en `factor()`/`actief()`/`is_actief()` nemen nu een `kanaal_id`.

Bij die derde zat één keuze die niet expliciet gesteld was en die ik zelf gemaakt heb: **een spawn-gebonden event op een kanaal dat géén spawn-kanaal is, laat daar tijdens het event tóch critters verschijnen** (`VangenCog._mag_spawnen()` kijkt naar `events.spawn_event_kanalen()`). Zonder dat zou een event op zo'n kanaal helemaal niets doen, en dan is "los van de spawn-kanalen" betekenisloos. Het houdt vanzelf weer op als het event afloopt — er valt niets op te ruimen.

Verdere gevolgen van per-kanaal:

- `factor()` **zonder** `kanaal_id` matcht alleen server-brede events. Bewust: de coins-berekening bij een gevecht kent geen kanaal en hoort niet ineens onder een event te vallen dat aan één spawn-kanaal hangt.
- `start()` negeert `kanaal_id` voor niet-spawn-gebonden types — een muntregen "in dit kanaal" bestaat niet.
- De aankondiging gaat bij een kanaal-gebonden event naar dát kanaal (plus het optionele extra kanaal), niet naar alle spawn-kanalen: daar gebeurt immers niets.
- Alleen de **activiteit-trigger** (`on_message`) kijkt naar event-kanalen, niet de tijd-trigger. Die draait als taak per geregistreerd spawn-kanaal en zou aan/af moeten worden gezet per event. Bewust niet gedaan: in een event-kanaal wordt juist gechat, en een incense hoort activiteit te belonen.
- Botsingsregel: hetzelfde type twee keer in hetzelfde kanaal mag niet, en een kanaal-event naast een server-breed event van dat type ook niet (dat overlapt per definitie). Verschillende types naast elkaar wél.

Vier nieuwe tests in `scripts/test_events.py` (kanaal-gebonden event lekt niet naar andere kanalen, server-breed geldt overal, muntregen negeert een kanaalkeuze, sterkte-omrekening heen en terug) plus uitbreiding van `test_events()` in `test_portal.py`. Suite (21 scripts) groen.

**Nog open uit dit gesprek**: het zeldzame item waarmee een speler zélf een incense kan activeren. Bewust nog niet gebouwd; de gebruiker wil dat dat item heel moeilijk te krijgen is, en dat is een aparte balansvraag.

## Bekende balans-issues

- ~~**Dagelijkse ranked-limiet blijft makkelijk te omzeilen met currency uit winst**~~ **Deels aangepakt (2026-07-27)**: "Extra match token" ging van 50 naar 150 Chaos Coins, plus kost nu 30x Maanschijnkristal + 2x Edelsteen (verder verhoogd tijdens de balans-audit, 2026-07-28) — een token kost dus niet meer alleen wat losse winst-currency, maar ook stevige, gerichte werk-tijd op Nachtwacht. Basisoorzaak (winnen levert currency op, currency koopt tokens) blijft bestaan — dit is frictie verhogen, geen structurele fix.
- ~~**XP-tempo tot MAX_LEVEL 50 was nooit tegen een doel afgezet**~~ **Opgelost (2026-07-28)**: zie "Balans-audit resultaat" hierboven.

## Belangrijke afspraken/voorkeuren van de gebruiker

- Altijd vragen stellen bij ontwerpkeuzes voordat je bouwt (niet zelf beslissen en doorbouwen).
- Na elke afgeronde stap automatisch committen + pushen naar `dev`, tenzij anders aangegeven.
- Testen gebeurt zoveel mogelijk via losse logica-scripts (met opruimen van testdata achteraf) i.p.v. de bot lokaal laten draaien, om botsingen met de live Railway-instance te voorkomen.
- Placeholder-balanswaarden (getallen voor genen, prijzen, `CURRENCY_PER_GRONDSTOF`, etc.) zijn expliciet gemarkeerd als voorlopig — later bij te stellen, mogelijk via een admin panel/web-portal.
